Name: energyanalysis-api
Host: 0.0.0.0
Port: 8062

# MySQL: 分析库(energy_analysis_db), 连接串由环境变量注入
MySQL:
  DataSource: ${ENERGY_ANALYSIS_MYSQL_DSN}

# Redis: 缓存日报/月报查询结果
Redis:
  Host: ${REDIS_ADDR}
  Type: node
  Pass: ${REDIS_PASS}
