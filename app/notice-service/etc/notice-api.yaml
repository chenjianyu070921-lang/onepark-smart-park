Name: notice-api
Host: 0.0.0.0
Port: 8094

# MySQL 连接配置 (DSN 由环境变量注入)
MySQL:
  DataSource: ${NOTICE_MYSQL_DSN}

# Redis 连接配置
Redis:
  Addr: ${REDIS_ADDR}
  Pass: ${REDIS_PASS}
  DB: 0

# Kafka 连接配置 (预留, 用于公告事件投递)
Kafka:
  Brokers: ${KAFKA_BROKERS}
