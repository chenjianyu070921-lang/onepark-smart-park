Name: visitor-api
Host: 0.0.0.0
Port: 8083

# MySQL 连接配置 (DSN 由环境变量注入)
MySQL:
  DataSource: ${VISITOR_MYSQL_DSN}

# Redis 连接配置
Redis:
  Addr: ${REDIS_ADDR}
  Pass: ${REDIS_PASS}
  DB: ${REDIS_DB}

# Kafka 连接配置 (预留, 用于访客事件投递)
Kafka:
  Brokers: ${KAFKA_BROKERS}
