# OnePark 智慧园区・深度进度盘点与单日任务分配（复核版 v2）

**盘点日期**：2026-09-17（复核，亲自跑 `go build` + 逐文件统计，不沿用旧报告结论）

**盘点方式**：
- 逐服务统计 `app/` 下 `.go` / logic / model / handler / server(gRPC) / mq / cron 文件数与 TODO
- 提取 GORM `TableName()`、HTTP `Path`、gRPC `rpc` 方法、Kafka topic 常量
- 检查 `common/`（24 个 .go）、`proto/`（14 个 .proto）、外层 `gateway/`
- **逐模块 `go build ./...` 实测**（go.work 共 22 个 module）

**编译实测结论（本次最关键发现）**：

| 结果 | 模块 |
|---|---|
| ✅ build OK（21 个） | access-control / alarm / billing / dashboard / device / dispatch / energy-analysis / energy-data / event-dispatcher / gateway-service / leasing / notice / parking / shadow / user-manage / video / visitor / workorder / common / gateway(外层) / proto |
| ❌ build FAIL（1 个） | **auth-service** —— `auth.go:13:2: string literal not terminated` |

> ⚠️ 旧报告称"17/17 全部 go build 通过"已**失真**。auth-service 当前根本编译不过，是今日全链路第一阻塞点。
> 精确病灶：`app/auth-service/auth.go` 第 13 行 import 被截断为 `"onepark/common/middlewar`（缺 `"` 和 `e`），第 21 行残留一个孤立 `"`。正确内容应为 `"onepark/common/middleware"`（该包与 `IdentityFromHeader` 函数均已存在于 `common/middleware/identity.go:14`）。

---

## 第一部分：六人六模块进度盘点表

| 负责人 | 模块名称（所含服务） | 完成度 | 已完成内容 | 缺失缺口 |
|---|---|---|---|---|
| **陈建羽** M1 物联接入 | device-service / shadow-service / event-dispatcher / gateway-service | **80%** | **HTTP 10 接口**：`/api/device/register`、设备 CRUD、`/command`、`/event`、产品 CRUD；**gRPC 4 方法**：Ping/SendCommand/GetDevice/GetDeviceStat；**表**：device、command_log、product、shadow；**MQ 消费**：telemetry 回写（上下线回写 device.status + 写 TDengine + 合并进 shadow.reported）；**cron**：指令超时扫描。shadow-service **gRPC 4 方法** Ping/GetShadow/UpdateDesired/UpdateReported（乐观锁）。event-dispatcher：EMQX MQTT 订阅 `onepark/device/#` → 分类投递 Kafka `device-telemetry` + 告警型额外投 `alarm-event`，含单测。gateway-service：TCP 换行 JSON 帧协议（auth/telemetry/event/status/ack/ping）+ bcrypt 设备认证 + 会话超时，含协议单测。 | device 表缺 type/latitude/longitude 列（`deviceserver.go:77,102` 两处 TODO，gRPC GetDevice 类型过滤返回零值）；event-dispatcher 与真实 EMQX 订阅**未联调**（代码就绪，环境未验证）；gateway 仅 TCP 帧，CoAP/HTTP 协议未实现；shadow 表与 device-service 内 shadow_model 双份定义待收敛。 |
| **成员3** M2 物业管理 | workorder / visitor / parking / notice | **82%** | **workorder HTTP 5 接口**：建单/详情/派单/状态流转/列表；**gRPC ListWorkOrders**（总数/待处理/今日新建/今日完成/平均时长/完成率/分页摘要，已做 tenant 过滤）；**表**：work_order、work_order_flow、work_order_attachment；**MQ**：消费 `alarm-event` 幂等建维修工单（uk_alarm_id + uk_order_no 双唯一键）、生产 `workorder-event`；**cron**：超时升级紧急；FSM 5 态 + 乐观锁 + 主表/流水同事务。**tenant 隔离已修复**：建单从 ctx 取 tenant_id 为空直接 400，派单 `WHERE id=? AND tenant_id=?`。visitor HTTP 4 接口（invite/checkin/checkout/list）；parking HTTP 4 接口（entry/exit/active/records）；notice HTTP 2 接口 + 消费 workorder-event。 | visitor 二维码核销状态机未补单测；parking 计费→notice 通知联动未联调；workorder gRPC 在 DB 未初始化时显式报错（设计如此，需 dashboard 侧降级）；parking/notice 无对外 gRPC。 |
| **成员4** M3 园区安防 | alarm / access-control / video | **55%** | **alarm HTTP 8 接口**：规则 CRUD/active 统计/列表/详情/状态；**gRPC GetActiveAlarms**（按 level 聚合，DB 空时降级返回空）；**表**：alarm、alarm_rule、alarm_operate_log、alarm_dlq；**规则引擎**（rule/ 目录，窗口计算）；**L1 幂等 + L2 Redis 冷却去重**（cooldown）；ES 检索；**WebSocket hub + redis relay** 广播；**消费 `alarm-event`**（15KB consumer，含死信/重试退避）；大量单测。access-control HTTP 4 接口（grant/revoke/remote-open/records）；**表**：access_permission、access_record、access_operate_log。video HTTP 3 接口（camera CRUD/stream）；表 camera。 | access-control 远程开门**只落库未调 M1 device SendCommand** gRPC；video `/stream/:id` 是桩，GB28181 未接入；alarm WebSocket 推送未与前端/dashboard wsserver 端到端联调；access-control gRPC 仅 Ping。 |
| **成员5** M4 能源管控 | energy-data / energy-analysis / billing | **60%** | energy-data HTTP 2 接口（history/realtime/:deviceId）；**gRPC GetDailyReport**；**表** energy_reading；MQ consumer 写入链路。energy-analysis HTTP 3 接口（daily/monthly/zone）。billing HTTP 4 接口（bills/generate/rule CRUD）；**表** billing_rule、bill；阶梯/峰谷电价 calc + cron 出账。 | energy-analysis **无对外 gRPC**（proto 仅 Ping）；billing proto 仅 Ping；TDengine 实际写入未实测；billing 的 energy_reading 与 energy-data 采集数据**未打通**（两张同名表分属两服务）；water 能耗字段未提供。 |
| **成员6** M5 运营招商+指挥调度 | leasing / dispatch / dashboard | **70%** | **dispatch HTTP 7 接口**：task CRUD/assign/status/staff upsert/list；**表**：dispatch_task、dispatch_staff、dispatch_task_log；**MQ**：消费 alarm-event 幂等建调度任务（uk_alarm_id）；**cron**：指派超时回收；派单打分引擎（技能>距离>负载）+ FSM。leasing HTTP 7 接口（contract CRUD/expiring/occupancy/bill auto）；**表** lease_contract、lease_zone、lease_bill、lease_contract_status_log；合同状态机 + cron。dashboard HTTP 2 接口（home/overview）；**provider 聚合**：workorder/alarm/energy 三路真实 gRPC 客户端（未配置时降级占位），Redis 聚合缓存 + wshub/wsserver。 | **dispatch 人工建单不写 tenant_id**（model 已有列，但 TaskCreateLogic 未赋值，落库为 0）——越权隐患仍在；dashboard **device provider 硬编码 notready**（device 虽有 GetDeviceStat gRPC，但聚合层未接，大屏设备卡片恒 null）；leasing/dispatch proto 仅 Ping；dashboard 四路并发聚合未实测。 |
| **成员2** M6 公共基础服务+DevOps | auth / user-manage / 外层 gateway | **65%** | **auth HTTP 4 接口**：login/refresh/validate/verify；**gRPC** Ping/Login/Verify/Refresh；表 sys_user、sys_user_role；JWT 双 token；启动强校验 JwtSecret（非本地空则 fatal）。**user-manage HTTP 11 接口**：users/roles/menus CRUD、role_menu 分配、权限校验；**gRPC** Ping/User CRUD；RBAC 五表（sys_user/sys_role/sys_menu/sys_role_menu/sys_user_role）。**外层 gateway 已存在**（`gateway/`，端口 8080）：Nacos 服务发现、JWT 全网关鉴权 + 注入 x-tenant-id/x-user-id/x-role-ids、反向代理、限流、访问日志，dev 默认放行/prod 强开。deploy 下 docker-compose + 全套 SQL（m1~m5/rbac/l2_tenant 迁移）。 | **auth-service 当前编译失败（P0 第一阻塞）**；gateway JWT 收口代码就绪但与下游 IdentityFromHeader **未端到端联调**；CI 仅最小化，无明文密钥扫描；user-manage 与网关鉴权联调记录待补。 |

**统计总量**：19 个 app 服务 + common + 外层 gateway + proto 共 22 个 module；约 30 张 GORM 表；HTTP 路由 70+ 条；业务 gRPC 方法分布在 device/shadow/alarm/workorder/energy/auth/user-manage（7 个服务）；6 个服务 proto 仅 Ping（access/billing/dashboard/dispatch/leasing/video）；common 公共包 16 个目录 24 个 .go。

---

## 第二部分：各模块可用性 & 跨服务支撑评估

### M1 物联接入（陈建羽）
- **可启动性**：device/shadow/event-dispatcher/gateway-service 均 build ✅；但依赖 MySQL+Redis+Kafka+EMQX 真实环境才能跑业务。
- **CRUD 可用性**：device 10 个 HTTP 接口 + 4 个 gRPC 已写全；shadow 三方法 gRPC 带乐观锁可调；gateway TCP 帧协议解析 + bcrypt 认证可用。
- **跨服务支撑**：shadow gRPC → M3/M5 读设备影子；event-dispatcher → alarm/workorder/dispatch 的 Kafka 上游；device SendCommand gRPC → parking/access-control 下发指令；device GetDeviceStat gRPC 已存在（但 dashboard 尚未接）。
- **卡点**：① event-dispatcher↔EMQX 未联调（主链路源头）；② device 表 type/lat/lng 列缺失导致 gRPC 类型过滤零值。**是主链路的起点卡点。**

### M2 物业管理（成员3）
- **可启动性**：workorder/visitor/parking/notice 全部 build ✅。
- **CRUD 可用性**：workorder 5 接口 + FSM + 乐观锁 + 流水审计 + 幂等建单完整；tenant 隔离已闭环。visitor/parking/notice 接口与 model 齐全。
- **跨服务支撑**：workorder gRPC ListWorkOrders → dashboard 大屏；消费 alarm-event 自动建单（主链路关键一环）；生产 workorder-event → notice/dashboard。
- **卡点**：visitor 核销状态机、parking→notice 联动未联调；其余不阻塞主链路。

### M3 园区安防（成员4）
- **可启动性**：alarm/access-control/video build ✅。
- **CRUD 可用性**：alarm 8 接口 + 规则引擎 + 去重 + WS hub 最完整；access-control 4 接口有完整 model；video stream 是壳。
- **跨服务支撑**：alarm gRPC GetActiveAlarms → dashboard；alarm-event 由 M1 投递、alarm 消费后判活并广播 WS。
- **卡点**：access-control 远程开门未回连 M1 SendCommand；video GB28181 未实现；alarm WS 推送未与前端/dashboard 联调。**alarm 消费→落库→WS 推送是主链路中段，需重点联调。**

### M4 能源管控（成员5）
- **可启动性**：energy-data/energy-analysis/billing build ✅。
- **CRUD 可用性**：history/realtime/daily/monthly/zone/bills/rule 接口可用；billing 出账 cron 可用。
- **跨服务支撑**：energy-data GetDailyReport gRPC → dashboard（已接 provider）。
- **卡点**：TDengine 写入未实测；energy-analysis/billing 无对外 gRPC；两张 energy_reading 表分属 data/analysis/billing 未统一。**对主链路影响小（大屏能源卡片可降级），不阻塞主链路。**

### M5 运营+调度（成员6）
- **可启动性**：dispatch/leasing/dashboard build ✅。
- **CRUD 可用性**：dispatch 7 接口 + 打分引擎 + 超时回收 + FSM 完整；leasing 7 接口 + 合同状态机可用；dashboard 2 接口有 provider 聚合框架 + 降级占位 + wsserver。
- **跨服务支撑**：dashboard 是大屏聚合终点，已真实接 workorder/alarm/energy 三路 gRPC；dispatch 消费 alarm-event 自动建调度单。
- **卡点**：① dispatch 人工建单漏写 tenant_id（越权隐患）；② dashboard device provider 恒 notready（设备卡片无数据）；③ 四路聚合并发与降级未实测。**dashboard 是主链路终点，联调重点。**

### M6 基础+DevOps（成员2）
- **可启动性**：user-manage、外层 gateway build ✅；**auth-service ❌ 编译失败，当前无法启动**。
- **CRUD 可用性**：user-manage 11 接口 + RBAC 五表可用；外层 gateway JWT 收口逻辑完整。
- **跨服务支撑**：auth JWT 签发/校验是全链路入口；外层 gateway 8080 统一收口鉴权 + 注入租户上下文（下游 IdentityFromHeader 提升 ctxdata）。
- **卡点**：**auth.go 语法错误是今日第一 P0**；gateway↔下游鉴权未端到端联调。**这是全链路第一个卡点（所有带鉴权请求都过这里）。**

---

## 第三部分：当日整体核心目标

**一句话目标**：先修 auth-service 编译错误，再端到端打通「设备 TCP/MQTT 上报 → gateway 认证 → shadow 写 reported → event-dispatcher 投 Kafka → alarm 判活 / workorder 自动建单 → dashboard `/overview` 聚合 → 大屏 WS 推送」主链路。

**主链路打通路径**：

```
设备上报(TCP/MQTT)
  → gateway-service 认证/解析帧
  → shadow-service gRPC UpdateReported 写影子
  → event-dispatcher 分类
  → Kafka: device-telemetry(全量) + alarm-event(告警型, 如 intrusion/fire)
  → alarm-service 规则引擎判活 → 落 alarm 表 + WebSocket 广播
  → workorder-service 消费 alarm-event → 幂等建维修工单
  → dashboard-service 并发 gRPC 聚合 workorder+alarm+energy
  → /overview 返回 + wsserver 推大屏
```

---

## 第四部分：分成员细分任务

### 陈建羽（M1 物联接入）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | event-dispatcher 与真实 EMQX 联调：发一条 MQTT 报文，确认 Kafka `device-telemetry` 与 `alarm-event` 收到 | 联调记录 + Kafka 消息截图 | 2h |
| **P0** | 打通 gateway→shadow：设备 TCP 上报 status，确认 shadow.reported 更新、device.status 回写 | 联调记录 + shadow 表数据截图 | 2h |
| P1 | device 表补 type/latitude/longitude 列，修 `deviceserver.go:77/102` 两处 TODO（GetDevice 类型过滤返回真实值） | DDL 迁移 + gRPC 返回真实值 | 1.5h |
| P2 | gateway CoAP 协议；shadow_model 双份定义收敛到 common | 协议文档 / 重构 PR | 排期 |

### 成员3（M2 物业管理）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | workorder 消费 `alarm-event` 联调：Kafka 收到告警后自动落一张待派维修工单（验证 uk_alarm_id 幂等） | 联调记录 + work_order 表截图 | 2h |
| **P0** | workorder gRPC ListWorkOrders 与 dashboard 联调：返回总数/待处理/今日新建 | 联调记录 | 1.5h |
| P1 | visitor 二维码核销状态机补单测 | 单测报告 | 2h |
| P2 | parking 计费→notice 通知联动 | 联调记录 | 排期 |

### 成员4（M3 安防）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | alarm 消费 `alarm-event`→落库→`GetActiveAlarms` 返回活跃数 联调 | 联调记录 | 2h |
| **P0** | alarm WebSocket 推送：告警产生→前端/dashboard wsserver 收到 | 推送验证截图 | 2h |
| P1 | access-control remote-open 回连 M1 device SendCommand gRPC（不再只落库） | 联调记录 | 2h |
| P2 | video GB28181/RTSP 接入技术调研 | 方案文档 | 排期 |

### 成员5（M4 能源管控）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | 验证 energy-data GetDailyReport 被 dashboard provider 调通（能源卡片有真实 kWh） | 联调记录 | 2h |
| P1 | billing 阶梯/峰谷电价 calc 单测补全；TDengine 实际写入实测 | 单测报告 + 写入验证 | 2h |
| P2 | energy-analysis/billing proto 补业务方法；统一 energy_reading 表归属 | proto 文件 | 排期 |

### 成员6（M5 运营+调度）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | 修 dispatch 人工建单漏写 tenant_id（TaskCreateLogic 从 ctxdata 取 tenant_id 并落库 + log 同样补） | 代码修复 + 越权单测 | 1.5h |
| **P0** | dashboard 并发聚合 workorder+alarm+energy 三路 gRPC，任一路失败降级返回部分数据（device 卡片暂 null + degraded） | 聚合代码 + 降级验证截图 | 3h |
| P1 | dispatch 消费 alarm-event 自动建调度单联调；超时回退后自动重派 | 联调记录 + 重派逻辑 | 1.5h |
| P2 | leasing/dispatch proto 补业务方法；dashboard 接 device GetDeviceStat | proto 文件 | 排期 |

### 成员2（M6 基础+DevOps）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | **修复 `auth-service/auth.go` 编译错误**：第13行改为 `"onepark/common/middleware"`，删除第21行孤立 `"`，`go build ./...` 通过 | 修复代码 + 编译通过证明 | 0.5h |
| **P0** | 外层 gateway JWT 鉴权联调：登录拿 token→带 token 访问下游→下游 ctxdata 取到 tenant/user | 联调记录 + 鉴权流程图 | 3h |
| P1 | user-manage RBAC 五表 SQL + 11 接口自测（登录→拿用户列表→角色/菜单/权限校验全通） | 建表 SQL + 接口自测记录 | 2h |
| P2 | CI 增加硬编码公网 IP/明文密码扫描 | CI 规则 | 排期 |

---

## 第五部分：全员统一时间安排

| 时段 | 时间 | 内容 |
|---|---|---|
| **上午（开发）** | 09:00–09:15 | 站会：通报本次实测发现（auth-service 编译失败为今日头号 P0）+ 对齐主链路目标 |
| | 09:15–12:00 | 成员2 先修 auth.go 并跑通 `go build`（30 分钟内必须绿）；陈建羽准备 EMQX/Kafka 联调环境；其余人并行做各自 P0 代码；**11:30 前完成"编译全绿"自检** |
| **下午（联调）** | 14:00–14:30 | 起齐 MySQL/Redis/Kafka/EMQX/Nacos；外层 gateway 8080 上线；确认 event-dispatcher 开始订阅投递 |
| | 14:30–17:30 | 按主链路顺序逐环打通：gateway→shadow→event-dispatcher→Kafka→alarm 判活+WS→workorder 自动建单→dashboard `/overview`；每通一环群内发截图 |
| **晚间（复盘）** | 17:30–18:00 | 各人报 P0 完成度、阻塞、更新遗留台账；确认全链路 6 步是否端到端跑通 |

---

## 第六部分：当日统一验收标准

### 单模块验收（每个服务）
- `go build ./...` 通过（**auth-service 必须上午第一个转绿**）
- P0 接口 curl 返回 200，参数校验/错误码走 errorx 统一响应
- 数据真实落库（MySQL/TDengine 有记录，非空壳）
- 关键路径有结构化日志

### 全链路验收（端到端 6 步，缺一不可）
1. 向 gateway-service 模拟一条设备 TCP/MQTT 上报；
2. shadow-service `reported` 字段更新（gRPC GetShadow 可见）；
3. event-dispatcher 向 Kafka `alarm-event` 投递（选 intrusion/fire 类）；
4. alarm-service 落一条活跃告警并 WebSocket 向前端推送；
5. workorder-service 自动落一张待派维修工单；
6. dashboard-service `/overview` 返回工单总数 +1 且 alarm 数 +1（能源卡片可降级）。

6 步全部手动走通 = 主链路验收通过。

### 遗留问题台账

| # | 问题 | 负责人 | 预计解决 |
|---|---|---|---|
| 1 | ~~auth-service/auth.go 编译失败（import 截断+孤立引号）~~ ✅ **已修复**：补回 `middleware` 闭合引号 + 丢失的 `authpb` 导入，`go build ./...` 通过（2026-09-17 实证 RC=0） | 成员2 | 今日上午 P0（0.5h） |
| 2 | event-dispatcher 与真实 EMQX 订阅未联调，Kafka 链路未端到端验证 | 陈建羽 | 今日下午 P0 |
| 3 | ~~gateway↔外层 JWT 鉴权未端到端联调，租户上下文未透传到下游~~ ✅ **已联调**：`gateway/internal/middleware/auth_chain_test.go` 复用真实中间件跑通"登录JWT→网关Auth→下游 `IdentityFromHeader`→ctxdata 取到 tenant/user/roles"，PASS；流程图+记录见 `docs/M6-网关鉴权收口联调记录.md` §3.3 | 成员2 | 今日下午 P0 |
| 4 | dashboard device provider 硬编码 notready，大屏设备卡片无数据 | 成员6 | 今日 P1 / 本周 P2 |
| 5 | dispatch 人工建单不写 tenant_id（model 有列，逻辑漏赋值） | 成员6 | 今日 P0 |
| 6 | access-control 远程开门未回连 M1 device SendCommand | 成员4 | 今日 P1 |
| 7 | video GB28181 未实现，`/stream/:id` 是桩 | 成员4 | 本周排期 |
| 8 | energy-analysis/billing/leasing/dispatch/access/video proto 仅 Ping | 成员4/5/6 | 本周排期 |
| 9 | TDengine 实际写入未实测；两张 energy_reading 表未统一 | 成员5 | 今日 P1 |
| 10 | alarm WebSocket 推送未与前端/dashboard wsserver 联调 | 成员4 | 今日下午 P0 |
| 11 | shadow_model 在 device-service 与 shadow-service 双份定义待收敛 | 陈建羽 | 排期 |
| 12 | visitor 核销状态机、parking→notice 联动未联调 | 成员3 | 今日 P1 / P2 |
| 13 | shadow-service pinglogic.go:27 残留 goctl 模板注释 | 陈建羽 | 随手清理 |

---

**数据来源**：22 个 module 逐目录统计 + 逐文件 GORM/路由/gRPC/Kafka 提取 + 逐模块 `go build ./...` 实测。本次与旧版最大差异：① auth-service 实际编译失败（旧版误判通过）；② workorder tenant 越权/丢租户问题**已修复**（旧版仍列未修）；③ 外层 gateway JWT 收口**已存在**（旧版误判"无独立网关目录"）。
