# 重点生检查（2026-09-23）

---

## 一、陈建羽（M1 物联接入底座）

### 重点生检查

陈建羽昨天任务中设备管理+事件分发存在以下bug：
1. event-dispatcher 集成测试覆盖不足，只有单元测试，没有端到端的"正常消息投递 + 坏消息DLQ死信"集成验证
2. EMQX 真实对接未联调，device-service MQTT 客户端连的是配置地址，实际 Broker 通不通、指令下行能不能发到设备，全没验证
3. MQTT 发布 QoS 未显式配置，代码里用默认值 QoS 0，对 QoS 0/1/2 区别和选型思路不清楚
4. gateway-service CoAP 协议栈只有代码，没有真实设备验证，连没连通过不知道

陈建羽不能利用三性六讲完成"设备注册→影子创建→遥测上报→事件分发"全链路讲解

陈建羽缺乏优化，只做了点 Redis 幂等去重，整体优化思路没有。还可以加：
- MySQL 侧：连接池参数、慢 SQL 钩子、设备表查询索引优化
- Redis 侧：设备影子缓存、热 key 治理、缓存穿透防护
- 接口侧：设备注册限流、参数校验统一化
- 链路侧：RequestID 全链路、MQTT message_id 与 RequestID 关联
- 中间件：gRPC 超时配置、设备指令超时熔断

陈建羽代码测试通过（10 个测试文件全过）

### 八股文检查

八股文存在的问题：
- MQTT QoS 级别区别：部分会（知道有 QoS，但说不清 0/1/2 具体区别和适用场景）
- Redis 缓存穿透/击穿/雪崩：部分会
- MySQL 索引优化：部分会
- gRPC 超时与重试：部分会
- Go 并发模型（goroutine + channel + GMP）：部分会

### 表达训练

今日训练了陈建羽的表达：
1. 三性六讲：给 10 分钟准备，讲解"设备注册→影子创建→遥测上报"链路，讲完后点评缺漏点
2. 模拟面试：抽 MQTT QoS、Redis 缓存问题、gRPC 超时三道题，要求结合 OnePark 项目作答

---

## 二、成员3（M2 物业管理 + 前端）

### 重点生检查

成员3昨天任务中工单+访客+前端存在以下bug：
1. workorder 没有 finished_at 字段，工单完成时间不记录，导致 avg_handle_sec 平均处理时长算不了
2. 前端页面太少，只有登录/布局/工单列表 3 页，告警、设备、能耗都没做
3. 前端与后端跨域 CORS 配置没验证，部署态接口能不能调通不知道
4. parking-service 没有 gRPC，大屏要停车数据只能查库或走 Kafka，没有标准 RPC 接口

成员3不能利用三性六讲完成"工单全生命周期+状态流转"完整讲解

成员3缺乏优化，只做了访客黑名单 fail-closed 安全修复，优化思路欠缺。还可以加：
- MySQL 侧：工单表复合索引、深分页优化、连接池参数
- Redis 侧：公告列表缓存、工单统计缓存、访客二维码临时存储
- 接口侧：工单创建限流、参数校验统一拦截器
- 存储侧：工单历史数据归档、操作日志审计表
- 前端：接口请求防抖、列表虚拟滚动、首屏加载优化

成员3代码测试通过（16 个测试文件全过）

### 八股文检查

八股文存在的问题：
- Go 语言优劣势：不会
- 单元测试方法（mock / 表驱动 / 基准测试）：部分会
- 代码覆盖率怎么测：只会一点
- Redis 数据结构及使用场景：部分会
- MySQL 事务隔离级别：不会

### 表达训练

今日训练了成员3的表达：
1. 三性六讲：给 10 分钟准备，讲解"工单创建→状态流转→升级→关闭"全流程
2. 模拟面试：抽 Go 优劣势、单元测试方法、Redis 数据结构三道题，结合 OnePark 项目说

---

## 三、成员4（M3 园区安防）

### 重点生检查

成员4昨天任务中告警服务存在以下bug：
1. alarm severity 口径和 M5 不一致，M3 是 1提示~4紧急（数字越大越重），M5 是 1 critical~3 minor（数字越小越重），方向反了
2. 测试覆盖极低，alarm-service 只有 3 个测试文件，consumer 消费逻辑、规则引擎表驱动、WebSocket 推送 全没测
3. access-control-service 没有 gRPC 契约定义，其他服务想调用门禁只能 HTTP 调或者直接查库
4. video-service 没有 gRPC，摄像头流地址、录像回放这些能力没法通过标准 RPC 对外提供

成员4不能利用三性六讲完成"设备遥测→规则评估→告警生成→通知推送"完整讲解

成员4缺乏优化，只有 Redis 幂等去重，优化思路不全。还可以加：
- MySQL 侧：告警表按时间分区、慢查询优化、索引优化（tenant_id + created_at 复合索引）
- Redis 侧：告警规则缓存、活跃告警统计缓存、冷却窗口 key 过期策略
- 接口侧：告警列表限流、ES 查询超时控制
- 链路侧：RequestID 从 MQTT 消息贯穿到告警落库
- 中间件：gRPC 超时配置、ES 连接池优化
- 存储：告警历史数据归档到 ES，MySQL 只存热数据

成员4代码测试通过（3 个测试文件全过，但覆盖太少）

### 八股文检查

八股文存在的问题：
- Go 语言优劣势：不会
- 代码覆盖率：只会一点
- 单元测试方法：部分会
- Kafka 消息丢失/重复消费怎么处理：部分会
- ES 倒排索引原理：不会
- Redis 缓存三大问题：部分会

### 表达训练

今日训练了成员4的表达：
1. 三性六讲：给 10 分钟准备，讲解"遥测消费→规则评估→告警生成→Kafka通知+WebSocket推送"全链路
2. 模拟面试：抽 Kafka 可靠性、Redis 幂等、ES 原理三道题，结合 OnePark 告警模块说

---

## 四、成员5（M4 能源管控）

### 重点生检查

成员5昨天任务中能源服务存在以下bug：
1. 测试覆盖极低，energy-data-service 和 energy-analysis-service 一个测试文件都没有，billing-service 也只有 calc 包有测试
2. TDengine 时序写入链路没联调，数据能不能写进去、查出来对不对，全不知道
3. energy-analysis-service 没有 gRPC 接口，大屏要能耗分析数据只能通过 energy-data 间接拿或者直接查库
4. 能源设备真实上报链路没打通，MQTT 遥测 → 能源读数 → 统计分析 这条链断的

成员5不能利用三性六讲完成"能源读数采集→时序存储→统计分析→账单生成"完整讲解

成员5缺乏优化，基本没做什么优化，思路也缺。还可以加：
- MySQL 侧：账单表索引、能源读数表分区、连接池参数
- Redis 侧：实时能耗缓存、统计结果缓存、计费规则缓存
- TDengine 侧：写入批处理、查询降采样、存储策略
- 接口侧：能耗查询限流、大查询分页/分段
- 中间件：gRPC 超时配置、TDengine 连接池
- 存储：冷热数据分离，历史数据降精度存储

成员5代码测试通过（只有 billing calc 1 个测试文件，覆盖严重不足）

### 八股文检查

八股文存在的问题：
- Go 语言优劣势：不会
- 代码覆盖率：只会一点
- 单元测试方法：部分会
- TDengine 时序数据库原理：不会
- Redis 持久化（RDB/AOF）：不会
- MySQL 索引原理（B+树）：只会一点

### 表达训练

今日训练了成员5的表达：
1. 三性六讲：给 10 分钟准备，讲解"能源读数→TDengine写入→日/月统计→账单生成"全链路
2. 模拟面试：抽 TDengine 原理、Redis 持久化、MySQL 索引三道题，结合 OnePark 能源模块说

---

## 五、成员6（M5 运营招商+指挥调度）

### 重点生检查

成员6昨天任务中大屏+调度存在以下bug：
1. dashboard 4 路 gRPC provider 没有降级超时的统一控制，某一路挂了会拖慢整个大屏接口
2. dispatch FSM 状态机边界测试不足，并发状态变更、跨级流转、取消恢复 这些场景没验证
3. leasing 合同到期续约 cron 没联调过，到期会不会自动提醒、会不会生成续约单，不知道
4. dashboard WebSocket 推送只做了基础的，没有消息积压处理、连接数限制、心跳检测

成员6不能利用三性六讲完成"告警事件→调度建单→人员分配→任务闭环"完整讲解

成员6缺乏优化，做了点 Redis 大屏缓存防击穿，但整体优化思路还不够。还可以加：
- MySQL 侧：调度任务表索引优化、合同表分区、大屏统计 SQL 优化
- Redis 侧：大屏统计结果缓存 + 主动刷新、调度人员池缓存、合同到期缓存
- 接口侧：大屏接口限流、任务创建限流
- 链路侧：RequestID 从 Kafka 消息贯穿到调度任务
- 中间件：gRPC 超时+重试配置、多路 provider 并发请求优化
- 性能：大屏 4 路 gRPC 并行调用优化（现在是不是串行的要看代码）

成员6代码测试通过（26 个测试文件，覆盖相对较好）

### 八股文检查

八股文存在的问题：
- Go 语言优劣势：不会
- 代码覆盖率：部分会
- 单元测试方法：部分会
- gRPC 负载均衡策略：不会
- Kafka 消费组重平衡机制：部分会
- Redis 集群模式：不会

### 表达训练

今日训练了成员6的表达：
1. 三性六讲：给 10 分钟准备，讲解"告警事件→自动建单→人员分配→任务完成"调度全链路
2. 模拟面试：抽 gRPC 负载均衡、Kafka 重平衡、Redis 集群三道题，结合 OnePark 说

---

## 六、徐家祥（M6 公共基础服务+DevOps）

### 重点生检查

徐家祥昨天任务中公共基础功能存在以下bug：
1. 限流中间件缺失，errorx 里定义了 M6-E-0007 限流错误码，但 common/middleware/ 下根本没有 ratelimit 实现，登录接口裸奔
2. user-manage 没接 Redis，权限查询每次都走五表联查数据库，QPS 上不去
3. 审计日志完全没有，登录登出、用户创建、权限变更这些关键操作全不留痕
4. gRPC Server 没配超时和拦截器，默认配置裸跑，下游 hang 住连接池就满了

徐家祥不能利用三性六讲完成"用户鉴权 + 网关中间件"功能的讲解

徐家祥缺乏优化，只做了点 Token 黑名单 Redis 相关的，整体优化思路没有。还可以加：
- MySQL 侧：连接池参数、慢 SQL 钩子、索引优化、分页优化
- Redis 侧：热 key/大 key 治理，缓存穿透/击穿/雪崩防护，权限令牌缓存，TTL 策略
- 接口侧：限流、参数校验、超时控制
- 链路侧：RequestID 全链路追踪、错误码规范化、审计日志
- 中间件：gRPC 超时、重试配置、统一拦截器
- 存储：软删除规范、审计日志、批量操作优化

徐家祥代码测试通过（auth 3 个 + user-manage 1 个 + common 若干，全过）

### 八股文检查

八股文存在的问题：
- Go 语言优势和劣势：完全不会
- 如何测试代码覆盖率：只会一点
- 单元测试有哪些方法：部分会
- MQTT / EMQX 相关：完全不会
- Redis 缓存穿透/击穿/雪崩：部分会
- MySQL 连接池、索引优化：只会一点
- gRPC 超时、错误处理：部分会

### 表达训练

今日训练了徐家祥的表达：
1. 三性六讲：给 10 分钟准备，重新用三性六讲框架讲解"用户鉴权 + 网关中间件"，讲完后点评哪里缺了、哪条线没串起来、少了哪些异常场景
2. 模拟面试训练：随机抽八股题，要求结合 OnePark 项目经验作答，不能空泛讲理论
