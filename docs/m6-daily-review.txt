徐家祥：
  昨天任务中 auth-service 登录功能存在空壳 bug（auth-service 只有占位路由，JWT 签发/校验全未实现，不知道 claims 里该放什么字段、Token 过期怎么处理、黑名单用什么存）
  昨天任务中网关鉴权链路未打通 bug（网关没接 JWT 中间件默认匿名放行，不知道鉴权在网关做还是服务做、各做哪一层、空密钥放行有多危险）
  昨天任务中基础设施连接池全面缺失 bug（MySQL、Redis、Kafka 三处都没配连接池，不知道连接池参数怎么设、Idle 和 Open 比例多少合理、ConnMaxLifetime 设多久）

  徐家祥不能利用三性六讲完成 RBAC 权限模块的功能讲解（讲不清"三性"是哪三性、"六讲"怎么套到权限模块上、每个性能点对应什么业务场景）

  徐家祥缺乏优化思路，只知道 Redis 能做缓存但说不出池化参数怎么配，还可以扩展这些方向：
    - MySQL 连接池优化（MaxIdleConns / MaxOpenConns / ConnMaxLifetime 怎么调）
    - Kafka Producer 超时与批量配置（WriteTimeout / BatchSize / Linger 怎么权衡）
    - Kafka Consumer 失败退避与死信队列（退避策略、死信路由、重试次数）
    - JWT 本地验签与权限缓存（避免每次校验都查 DB）

  徐家祥代码测试未通过：
    - auth-service 登录接口测试：请求后返回空响应，无 Token 签发
    - 网关鉴权测试：不带 Token 请求仍然 200 通过，未被拦截
    - Kafka 消费可靠性测试：handler 失败后无限重试，CPU 飙升

  八股文检查（检查所有重点生）
    - JWT 的组成部分？优缺点？怎么实现 Token 失效？
    - RBAC 权限模型怎么设计？有几种实现方式？
    - Kafka 怎么保证消息不丢？ISR 机制讲一下？

  八股文存在的问题：
    - 不会：Kafka ISR 完整机制（能说出 ISR 但讲不清 HW/LEO、ISR 收缩和扩张条件）
    - 部分会：JWT 失效（知道黑名单但说不出为什么用 Redis、过期怎么清理、和 Refresh Token 怎么配合）、RBAC 设计（能说出五表但讲不清权限粒度、角色继承、多租户隔离）
    - 基本会：（暂无全对的，三道都有缺口）

  表达训练
  今天训练了徐家祥的表达，训练方式：
    1. 三性六讲解题训练：给 10 分钟准备，用三性六讲框架讲解"RBAC 权限模块设计"，讲完后点评哪里缺了、哪条线没串起来
    2. 现场问答训练：随机抽八股题，要求先给结论再展开，不能绕圈子，控制在 1 分钟内答完
