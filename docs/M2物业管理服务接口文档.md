# M2 物业管理服务模块 · 接口文档

> **模块名称：** M2 物业管理服务（Property Service）
> **版本：** V1.2
> **更新时间：** 2026-09-20
> **接口范围：** 4 个微服务、共 **23 个接口**（22 个 HTTP + 1 个 gRPC）
> **对齐依据：** `app/*/*.api`（HTTP 契约）、`proto/workorder/workorder.proto`（gRPC 契约）、`common/response`、`common/errorx`、各服务 `internal/logic` 实际实现、`docs/M2网关路由与端口清单.md`
> **适用读者：** 前端/大屏开发、联调与测试、M5/M6 等对接方

---

## 一、服务与接入信息

### 1.1 服务端口

| 服务 | 内网 HTTP | gRPC | 网关前缀 | 部署目录 |
| --- | --- | --- | --- | --- |
| workorder-service 工单 | `8091` | `9091` | `/api/workorder`、`/api/workorders` | `app/workorder-service` |
| visitor-service 访客 | `8092` | — | `/api/visitor`、`/api/visitors` | `app/visitor-service` |
| parking-service 停车 | `8093` | — | `/api/parking` | `app/parking-service` |
| notice-service 公告 | `8094` | — | `/api/notice`、`/api/notices` | `app/notice-service` |

### 1.2 接入地址

| 场景 | Base URL | 说明 |
| --- | --- | --- |
| **推荐：经网关** | `http://<gateway-host>:8080` | 统一入口 `gateway/`，按最长前缀反向代理到上表内网端口，并注入上下文头 |
| 本地联调：直连服务 | `http://127.0.0.1:809x` | 跳过网关，需自行携带 `x-tenant-id` 等 Header |

> 网关路由表配置于 `gateway/etc/apigateway-api.yaml` 的 `Upstreams`；未匹配前缀返回 `404 {code:"M6-E-0004"}`，上游不可用返回 `502 {code:"M6-E-0006"}`。

---

## 二、通用约定

### 2.1 请求头（由网关注入，服务侧只读）

| Header | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `x-tenant-id` | int64 | 是（经网关可缺省） | 园区/租户 ID，**所有业务数据按此隔离**。经网关缺省为 `DefaultTenantId`（联调默认 `1`） |
| `x-user-id` | int64 | 否 | 当前用户 ID（工单发起人、公告发布人等），缺省 `0` |
| `x-role-ids` | string | 否 | 角色 ID 列表，逗号分隔；供后续行级权限扩展 |
| `x-request-id` | string | 否 | 全链路追踪 ID；缺省时网关生成并回写响应头，同时作为 Kafka 事件的 `event_id` |
| `Content-Type` | string | 是（写接口） | 固定 `application/json` |

> 服务侧由 `common/middleware.Tenant` 读取上述头写入 `common/ctxdata`，logic 层通过 `ctxdata.GetTenantId/GetUserId/GetRoleIds` 读取。**直连服务时若缺 `x-tenant-id`，业务会返回 `M6-E-0001`（缺少租户信息）**。
>
> ⚠️ 当前网关为**无鉴权联调模式**（未接 M6 JWT）。接入 M6 `ValidateToken/CheckPermission` 后，`x-user-id`/`x-role-ids`/`x-tenant-id` 将由鉴权中间件按 Token 解析并覆盖缺省值。

### 2.2 统一响应体

所有业务接口（成功与业务失败）统一返回：

```json
{
  "code": "0",
  "msg": "ok",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | string | `"0"` 表示成功；非 `"0"` 为业务错误码（格式 `{模块}-{级别}-{4位数字}`） |
| `msg` | string | 提示信息，成功固定 `"ok"` |
| `data` | object / array | 业务数据载荷，失败时不返回该字段 |

### 2.3 HTTP 状态码与错误码映射（**重点**）

错误码级别决定 HTTP 状态码（`common/errorx/http_status.go`）：

| 错误码级别 | HTTP 状态码 | 触发示例 |
| --- | --- | --- |
| `-E-`（Error） | **500** | `M2-E-1001` 工单不存在、`M6-E-0001` 缺少租户信息、`M2-E-5001` 内部错误 |
| `-W-`（Warn） | **400** | 参数/状态类提示（M2 当前未启用） |
| `-I-`（Info） | 200 | 提示类（M2 当前未启用） |

> ⚠️ **联调须知**：M2 现有业务错误码均为 `-E-` 级，因此"工单不存在"等**正常业务失败会返回 HTTP 500**，但响应体仍是统一信封。**前端判定业务结果请以 body 的 `code` 字段为准，不要只看 HTTP 状态码。**

### 2.4 参数校验失败（非统一信封）

请求参数不满足 go-zero 校验（必填缺失、类型错误）时，handler 直接走 `httpx.ErrorCtx`，返回 go-zero **默认错误响应**（约 HTTP 400，形如 `{"error":"..."}`），**不是** `{code,msg,data}` 信封。

**必填判定规则**：`.api` 中字段未标注 `optional` 即为**必填**，语义是**该字段必须出现在请求中**（query 必须带该 key、JSON body 必须包含该字段）；**缺失**时校验失败返回 400。例如不传 `page`、`page_size`、`plate_no` 等均会 400。

> ⚠️ **重要**：go-zero 的必填校验是"**键必须存在**"，**不是**"值不得为 0"。因此显式传 `page=0`、`vehicle_type=0`、`type=0` **会通过校验并进入业务逻辑**：
> - 分页字段（`page`/`page_size`）由 logic 层兜底（见 2.5），无副作用；
> - 枚举字段（`type`/`priority`/`vehicle_type`）传 `0` 会作为**非法值写入数据库**，调用方必须按各接口的取值约束传参（枚举一律 ≥ 1）。

### 2.5 分页约定

| 参数 | 类型 | 必填 | 缺失时 | 传 0 时 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `page` | int64 | 是 | 400 | 业务层兜底为 `1` | 页码，从 1 开始 |
| `page_size` | int64 | 是 | 400 | 业务层兜底为 `10` | 每页条数 |

分页响应统一结构：

```json
{ "total": 100, "list": [] }
```

- `total`：满足筛选条件的**总条数**（非当前页条数）
- `list`：当前页数据，**恒为数组**（空结果返回 `[]`，不返回 `null`）

各列表默认排序见对应接口说明。

### 2.6 时间与金额约定

| 类型 | 约定 |
| --- | --- |
| **时间** | 统一为**秒级 Unix 时间戳（int64）**，如 `1757894400`；`0` 表示该时间不存在/未设置 |
| **金额** | 统一为 **decimal 字符串**（两位小数），如 `"15.00"`，避免浮点精度误差；无费用时为 `"0.00"` |
| **枚举** | 统一为 `int8` 数值，字典见[附录 A](#附录-a枚举字典) |

---

## 三、接口总览

| # | 服务 | 方法 | 路径（经网关） | 功能 | 章节 |
| --- | --- | --- | --- | --- | --- |
| 1 | workorder | POST | `/api/workorder` | 创建工单（建单即待派单） | [4.1.1](#411-创建工单) |
| 2 | workorder | PUT | `/api/workorder/:id/assign` | 派单（待派单→处理中） | [4.1.2](#412-派单) |
| 3 | workorder | PUT | `/api/workorder/:id/status` | 状态流转（submit/approve/reject/close） | [4.1.3](#413-工单状态流转) |
| 4 | workorder | GET | `/api/workorder/:id` | 工单详情 | [4.1.4](#414-工单详情) |
| 5 | workorder | GET | `/api/workorders` | 工单分页列表 | [4.1.5](#415-工单分页列表) |
| 6 | workorder | gRPC | `WorkorderService/ListWorkOrders`（`:9091`） | 供 M5 大屏聚合统计 | [4.1.6](#416-grpc-listworkorders) |
| 7 | visitor | POST | `/api/visitor/invite` | 发起访客邀请，生成二维码（多方式核验 + 黑名单实时拦截） | [4.2.1](#421-发起访客邀请) |
| 8 | visitor | POST | `/api/visitor/checkin` | 访客签入（多方式核验 + 黑名单拦截 + 核销 + 调 M1 开门） | [4.2.2](#422-访客扫码签入) |
| 9 | visitor | POST | `/api/visitor/checkout` | 访客签出 | [4.2.3](#423-访客签出) |
| 10 | visitor | GET | `/api/visitors` | 访客记录分页列表 | [4.2.4](#424-访客记录分页列表) |
| 11 | visitor | GET | `/api/visitor/:id` | 访客详情 + 门禁进出轨迹（P2） | [4.2.5](#425-访客详情与门禁轨迹) |
| 12 | visitor | POST | `/api/visitor/blocklist` | 访客黑名单新增/更新（P2，实时拦截用） | [4.2.6](#426-访客黑名单新增更新) |
| 13 | visitor | POST | `/api/visitor/blocklist/:id/unblock` | 访客黑名单解除（P2） | [4.2.7](#427-访客黑名单解除) |
| 14 | parking | POST | `/api/parking/entry` | 车辆入场 | [4.3.1](#431-车辆入场) |
| 15 | parking | POST | `/api/parking/exit` | 车辆离场（计费） | [4.3.2](#432-车辆离场计费) |
| 16 | parking | GET | `/api/parking/records` | 停车记录分页列表 | [4.3.3](#433-停车记录分页列表) |
| 17 | parking | GET | `/api/parking/active` | 在场车辆列表 | [4.3.4](#434-在场车辆列表) |
| 18 | notice | POST | `/api/notice` | 发布公告（立即/定时） | [4.4.1](#441-发布公告) |
| 19 | notice | GET | `/api/notices` | 公告分页列表 | [4.4.2](#442-公告分页列表) |
| 20 | notice | GET | `/api/notices/unread-count` | 站内信未读计数（P2） | [4.4.3](#443-站内信未读计数) |
| 21 | notice | POST | `/api/notices/read`、`/api/notice/:id/read` | 公告已读回填（P2，两等价路由） | [4.4.4](#444-公告已读回填) |
| 22 | notice | POST | `/api/notice/:id/recall` | 公告撤回（P2：已发布→已撤回 + 补偿事件） | [4.4.5](#445-公告撤回) |

---

## 四、接口详细定义

## 4.1 workorder-service（工单服务）

> 工单状态机见[附录 A.1](#a1-工单)；所有写操作均带 `tenant_id` 与**乐观锁 `version`** 校验。

### 4.1.1 创建工单

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/workorder` |
| 直连地址 | `http://127.0.0.1:8091/api/workorder` |
| Handler / Logic | `CreateWorkOrderHandler` / `CreateWorkOrderLogic` |
| 权限 | 业主/企业租户、物业客服、园区管理员 |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `type` | int8 | 是 | 工单类型：1 报修、2 投诉、3 巡检、4 保洁、5 装修、6 搬运、7 其他；**取值必须为 1~7** |
| `title` | string | 是 | 工单标题 |
| `description` | string | 是 | 详细描述 |
| `priority` | int8 | 是 | 优先级：1 紧急、2 普通、3 低；**取值必须为 1~3** |
| `location` | string | 是 | 位置/楼栋房号 |

> `tenant_id`、`reporter_id`（发起人）由网关注入 Header，**请求体不需要传**。

**请求示例**

```json
{
  "type": 1,
  "title": "3 号楼 2 单元水管漏水",
  "description": "厨房主水管接口处持续渗水，需要紧急处理",
  "priority": 1,
  "location": "3 号楼 2 单元 502"
}
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 工单主键 ID |
| `order_no` | string | 工单号，格式 `WO-YYYYMMDD-XXXX`（如 `WO-20260915-0431`） |
| `status` | int8 | 当前状态，建单固定为 `0`（待派单） |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": { "id": 1001, "order_no": "WO-20260915-0431", "status": 0 }
}
```

**业务规则**

1. 建单即进入 `待派单(0)`，`assignee_id`/`department_id` 默认 `0`，乐观锁 `version` 初始 `0`。
2. 同步写入建单流水 `work_order_flow`（`from_status=-1`、`action="create"`）。
3. 异步发布 Kafka 事件 `workorder-event`（`event="created"`），**尽力而为**：Kafka 不可用时仅记日志，不影响建单成功。

**错误码**

| code | HTTP | 场景 |
| --- | --- | --- |
| `M6-E-0001` | 500 | 缺少租户信息（直连未带 `x-tenant-id`） |
| `M2-E-5001` | 500 | 写库失败等内部错误 |

---

### 4.1.2 派单

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `PUT /api/workorder/:id/assign` |
| Handler / Logic | `AssignWorkOrderHandler` / `AssignWorkOrderLogic` |
| 权限 | 物业客服、园区管理员 |

**路径参数**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 工单 ID |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `assignee_id` | int64 | 是 | 处理人 ID（维修人员）；应为有效用户 ID（≥1） |
| `department_id` | int64 | 否 | 处理部门 ID |

**请求示例**

```json
{ "assignee_id": 2001, "department_id": 12 }
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 工单 ID |
| `order_no` | string | 工单号 |
| `status` | int8 | 流转后状态 `1`（处理中） |

**业务规则**

1. FSM 校验：仅允许 `待派单(0)` --assign--> `处理中(1)`。
2. **乐观锁**：更新条件为 `id + tenant_id + version`，并置 `version = version + 1`；
   `RowsAffected == 0` 即判定为并发冲突，返回 `M2-E-1003`，前端应提示"刷新后重试"。
3. 写派单流水 + 发布 `workorder-event`（`event="assigned"`，key=工单号）。

**错误码**

| code | HTTP | 场景 |
| --- | --- | --- |
| `M2-E-1001` | 500 | 工单不存在（或不属于当前租户） |
| `M2-E-1002` | 500 | 当前状态不允许派单（非待派单） |
| `M2-E-1003` | 500 | 派单冲突（并发修改，`version` 不匹配） |
| `M2-E-5001` | 500 | 内部错误 |

---

### 4.1.3 工单状态流转

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `PUT /api/workorder/:id/status` |
| Handler / Logic | `UpdateWorkOrderStatusHandler` / `UpdateWorkOrderStatusLogic` |
| 权限 | `submit`：维修人员；`approve`/`reject`：报修人/客服；`close`：客服、管理员 |

**路径参数**：`id`（int64，工单 ID）

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `action` | string | 是 | 流转动作：`submit` / `approve` / `reject` / `close` |
| `remark` | string | 否 | 流转备注（验收意见、驳回原因等） |

**动作与状态迁移**

| action | 含义 | 允许的 from 状态 | to 状态 |
| --- | --- | --- | --- |
| `submit` | 提交完成 | 处理中(1) | 待验收(2) |
| `approve` | 验收通过 | 待验收(2) | 已完成(3) |
| `reject` | 驳回 | 待验收(2) | 处理中(1) |
| `close` | 关闭/取消 | 待派单(0)、处理中(1)、待验收(2)、已完成(3) | 已关闭(4) |

> `已关闭(4)` 为终态，**无任何出边**，不可再流转。

**请求示例**

```json
{ "action": "approve", "remark": "处理及时，验收通过" }
```

**响应参数（`data`）**：同 4.1.2（`id`、`order_no`、`status`）

**业务规则**

1. FSM 校验失败 → `M2-E-1002`。
2. 乐观锁更新，冲突 → `M2-E-1003`。
3. 目标状态为 `已完成(3)` 或 `已关闭(4)` 时，同步写入 `finished_at`。
4. 写流转流水 + 发布 `workorder-event`（`event="status_changed"`，含 `from_status`/`to_status`）。

**错误码**：`M2-E-1001`、`M2-E-1002`、`M2-E-1003`、`M2-E-5001`（映射同 4.1.2）

---

### 4.1.4 工单详情

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/workorder/:id` |
| Handler / Logic | `GetWorkOrderHandler` / `GetWorkOrderLogic` |

**路径参数**：`id`（int64）

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 工单 ID |
| `tenant_id` | int64 | 园区 ID |
| `order_no` | string | 工单号 |
| `type` | int8 | 工单类型（见附录 A.1） |
| `title` | string | 标题 |
| `description` | string | 描述 |
| `reporter_id` | int64 | 报修/发起人 ID |
| `assignee_id` | int64 | 处理人 ID，未派单为 `0` |
| `department_id` | int64 | 处理部门 ID |
| `status` | int8 | 当前状态 |
| `priority` | int8 | 优先级 |
| `location` | string | 位置 |
| `attachments` | string | MinIO 对象 Key 列表（JSON 字符串），无附件为空串 |
| `version` | int64 | 乐观锁版本号 |
| `created_at` | int64 | 创建时间（秒级） |
| `updated_at` | int64 | 更新时间（秒级） |
| `finished_at` | int64 | 完成/关闭时间（秒级），未完成时为 `0` |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": {
    "id": 1001,
    "tenant_id": 1,
    "order_no": "WO-20260915-0431",
    "type": 1,
    "title": "3 号楼 2 单元水管漏水",
    "description": "厨房主水管接口处持续渗水",
    "reporter_id": 3001,
    "assignee_id": 2001,
    "department_id": 12,
    "status": 2,
    "priority": 1,
    "location": "3 号楼 2 单元 502",
    "attachments": "",
    "version": 2,
    "created_at": 1757894400,
    "updated_at": 1757898000,
    "finished_at": 0
  }
}
```

**错误码**：`M2-E-1001`（工单不存在）、`M2-E-5001`

---

### 4.1.5 工单分页列表

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/workorders` |
| Handler / Logic | `ListWorkOrdersHandler` / `ListWorkOrdersLogic` |

**Query 参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `status` | int8 | 否 | 状态筛选，`0`/缺省表示不限 |
| `type` | int8 | 否 | 类型筛选，`0`/缺省表示不限 |
| `assignee_id` | int64 | 否 | 处理人筛选，`0`/缺省表示不限 |
| `page` | int64 | 是 | 页码，≥1 |
| `page_size` | int64 | 是 | 每页条数，≥1 |

**请求示例**

```
GET /api/workorders?status=1&page=1&page_size=10
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `total` | int64 | 符合条件总数 |
| `list` | array | 工单列表项 |

列表项 `WorkOrderItem`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 工单 ID |
| `order_no` | string | 工单号 |
| `type` | int8 | 类型 |
| `title` | string | 标题 |
| `status` | int8 | 状态 |
| `priority` | int8 | 优先级 |
| `assignee_id` | int64 | 处理人 ID |
| `created_at` | int64 | 创建时间（秒级） |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": {
    "total": 37,
    "list": [
      {
        "id": 1001,
        "order_no": "WO-20260915-0431",
        "type": 1,
        "title": "3 号楼 2 单元水管漏水",
        "status": 1,
        "priority": 1,
        "assignee_id": 2001,
        "created_at": 1757894400
      }
    ]
  }
}
```

**业务规则**

1. 强制按 `tenant_id` 隔离，未传筛选条件时返回本园区全量。
2. 排序：`id DESC`（最新在前）。
3. 列表项**不含**大字段（`description`、`attachments`），避免列表接口过载。

**错误码**：`M2-E-5001`（查询失败）；⚠️ DB 未初始化时当前会 panic → 500，见[附录 E](#附录-e已知限制与注意事项) 第 3 条；`page`/`page_size` 缺失返回 go-zero 默认 400

---

### 4.1.6 gRPC ListWorkOrders

| 项 | 值 |
| --- | --- |
| Service / Method | `onepark.workorder.WorkorderService/ListWorkOrders` |
| 地址 | `127.0.0.1:9091`（配置见 `app/workorder-service/etc/workorder-grpc.yaml`） |
| 调用方 | M5 dashboard-service（大屏聚合，**不经网关**） |
| 契约文件 | `proto/workorder/workorder.proto` |

**请求消息 `ListWorkOrdersReq`**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tenant_id` | int64 | 园区 ID，`0` 表示不限 |
| `status` | int32 | 状态过滤，`0` 表示不限 |
| `page` | int64 | 页码 |
| `page_size` | int64 | 每页大小 |

**响应消息 `ListWorkOrdersResp`**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `total` | int64 | 符合条件总数 |
| `list` | repeated `WorkOrderSummary` | 当前页工单摘要 |
| `pending_count` | int64 | **待处理工单数（待派单 + 处理中）** |
| `today_count` | int64 | 今日新建工单数 |
| `completed_today` | int64 | 今日完成（已完成状态）工单数 |
| `avg_process_minutes` | double | 已完成工单平均处理时长（分钟） |
| `completion_rate` | double | 完成率（`0~100`，已完成/总数） |

`WorkOrderSummary`：`id`、`order_no`、`type`、`status`、`priority`、`assignee_id`、`created_at`

**错误行为（重要）**

- 当数据库未就绪（`DB == nil`）时，**返回 gRPC 错误 `M2-E-5001`**，而非静默返回全 `0` 统计，避免大屏把 `0` 误认为真实值。
- dashboard 侧对每路数据源独立降级：该路失败 → 对应字段返回 `null` 并在 `fallback_markers` 中标注来源。

**调用示例（grpcurl）**

```bash
grpcurl -plaintext -d '{"tenant_id":1,"status":0,"page":1,"page_size":10}' \
  127.0.0.1:9091 onepark.workorder.WorkorderService/ListWorkOrders
```

---

## 4.2 visitor-service（访客服务）

> 访客状态机见[附录 A.2](#a2-访客)。

### 4.2.1 发起访客邀请

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/visitor/invite` |
| Handler / Logic | `VisitorInviteHandler` / `VisitorInviteLogic` |
| 权限 | 业主/企业租户、物业客服、园区管理员 |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `visitor_name` | string | 是 | 访客姓名 |
| `visitor_phone` | string | 是 | 访客手机号（核验方式 2 或黑名单手机号维度时使用） |
| `visit_time` | int64 | 是 | 预期到访时间（秒级时间戳），需为有效时间（>0）；传 `0` 将导致记录无预期到访时间 |
| `expire_time` | int64 | 是 | 二维码过期时间（秒级时间戳），需为有效时间（>0）；传 `0` 将导致记录**永不过期**（签入侧不再做过期判定） |
| `reason` | string | 否 | 来访原因 |
| `verify_channel` | int8 | 否 | 核验方式：`1` 二维码（默认）/ `2` 手机号 / `3` 身份证 / `4` 人脸 / `5` 工牌；`0` 或缺失按 `1` 处理 |
| `id_no` | string | 否 | 身份证号（核验方式 `3` 时必填，落库便于按身份证核验；**当前明文存储，加密方案待组长确认**） |
| `face_token` | string | 否 | 人脸特征令牌（核验方式 `4` 时必填；**当前明文存储，加密方案待组长确认**） |
| `badge_no` | string | 否 | 工牌号（核验方式 `5` 时必填） |

**请求示例**

```json
{
  "visitor_name": "张三",
  "visitor_phone": "13800000000",
  "visit_time": 1757905200,
  "expire_time": 1757991600,
  "reason": "探亲访友"
}
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 访客记录 ID |
| `qr_code` | string | 加密二维码内容（内含签名与有效期），由前端渲染成二维码下发给访客 |
| `expire_at` | int64 | 过期时间（秒级，回显请求值） |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": {
    "id": 5001,
    "qr_code": "eyJleHBpcmUiOjE3NTc5OTE2MDAsImludml0ZXJfaWQiOjMwMDEsInRlbmFudF9pZCI6MSwidmlzaXRvciI6IuW8oOS4iSJ9fDlkM2YxYjJj",
    "expire_at": 1757991600
  }
}
```

**业务规则**

1. 二维码内容 = `base64( JSON载荷 + "|" + 签名 )`，签名为 `md5(tenant_id|inviter_id|visitor_name|expire|salt)`，可被门岗校验、防伪造。
2. 记录初始状态 `1`（待使用），`blacklisted = 0`；`verify_channel` 未传或 `0` 时默认 `1`（二维码）。
3. 邀请人 `inviter_id` 取自网关 `x-user-id`。
4. **黑名单实时拦截（P2）**：邀请阶段即按 `visitor_phone` / `id_no` 查询 `visitor_blocklist`（同租户、生效中、在生效时间窗内），命中则返回 `M2-E-2004` 并禁止生成通行码，从源头阻止被拉黑人员入园。
5. `id_no` / `face_token` 当前为**明文占位列**（迁移已执行），加密存储方案待组长确认后另行增量迁移。

**错误码**：`M6-E-0001`（缺少租户信息）、`M2-E-2004`（访客被拉黑禁止邀请）、`M2-E-5001`

---

### 4.2.2 访客扫码签入

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/visitor/checkin` |
| Handler / Logic | `VisitorCheckinHandler` / `VisitorCheckinLogic` |
| 权限 | 访客（门岗终端）、保安 |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `qr_code` | string | 否 | 加密二维码内容（核验方式 `1` 二维码时必填） |
| `verify_channel` | int8 | 否 | 核验方式：`1` 二维码（默认）/ `2` 手机号 / `3` 身份证 / `4` 人脸 / `5` 工牌；`0` 或缺失按 `1` 处理 |
| `credential` | string | 否 | 非二维码方式的凭证值：方式 `2` 传手机号、`3` 传身份证号、`4` 传人脸令牌、`5` 传工牌号 |

> 二维码方式必须带 `qr_code`；其余方式必须带 `credential`，且记录自身的 `verify_channel` 须与请求方式一致，否则返回 `M2-E-2005`。

**请求示例**

```json
// 二维码签入
{ "qr_code": "eyJleHBpcmUiOjE3NTc5OTE2MDAsImludml0ZXJfaWQiOjMwMDEsInRlbmFudF9pZCI6MSwidmlzaXRvciI6IuW8oOS4iSJ9fDlkM2YxYjJj" }
// 身份证签入(核验方式=3)
{ "verify_channel": 3, "credential": "11010119900307XXXX" }
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 访客记录 ID |
| `status` | int8 | 签入后状态，固定 `2`（已签入） |
| `checkin_at` | int64 | 签入时间（秒级） |
| `device_id` | string | 实际开门设备 ID；M1 未配置或不可达时为空串 |
| `open_msg` | string | 开门结果提示；M1 降级时提示"请联系前台人工开门"，不阻断签入 |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": { "id": 5001, "status": 2, "checkin_at": 1757905300, "device_id": "door-gate-01", "open_msg": "开门成功" }
}
```

**处理流程**

1. 确定核验方式（缺省 `1` 二维码）；
2. **二维码方式**：`constant-time` 验签（防伪造）→ 按 `qr_code + tenant_id` 查记录；其余方式：按 `verify_channel` 映射字段（`visitor_phone`/`id_no`/`face_token`/`badge_no`）查记录，**跳过二维码验签**；
3. 校验记录自身 `verify_channel` 与请求方式一致 → 是否存在 → 是否过期 → 是否已核销；
4. **黑名单实时拦截（P2）**：命中 `visitor_blocklist`（生效中且时间窗内）则将该记录 `blacklisted` 置 `1` 并返回 `M2-E-2004`，拒绝通行；
5. 置为 `2`（已签入）并写 `checkin_at`（**一次性核销，CAS 防并发重复开门**）；
6. 调 M1 `DeviceService.SendCommand(open_door)`（**800ms 短超时**），成功后回填 `device_id`；
7. **降级策略**：M1 不可达或未配置时仅记日志，**签入仍然成功**，`device_id` 为空、`open_msg` 提示人工兜底 —— 前端不应因 `device_id` 为空而判定签入失败。

**错误码**

| code | HTTP | 场景 |
| --- | --- | --- |
| `M2-E-2001` | 500 | 二维码已过期（同时将记录置为 `4` 已过期） |
| `M2-E-2002` | 500 | 二维码无效（记录不存在）或已被核销（已签入/已签出） |
| `M2-E-2003` | 500 | 签入写库失败 |
| `M2-E-2004` | 500 | 访客被拉黑，禁止通行（邀请/签入实时拦截） |
| `M2-E-2005` | 500 | 不支持的核验方式或凭证缺失/不匹配（多方式核验） |
| `M2-E-5001` | 500 | 内部错误 |

---

### 4.2.3 访客签出

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/visitor/checkout` |
| Handler / Logic | `VisitorCheckoutHandler` / `VisitorCheckoutLogic` |
| 权限 | 访客（门岗终端）、保安 |

**请求参数（JSON Body，`id` 与 `qr_code` 二选一）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | int64 | 否 | 访客记录 ID（优先使用） |
| `qr_code` | string | 否 | 加密二维码内容 |

> 两者均为 `optional`，但**至少传一个**，否则返回 `M6-E-0001`（缺少 id 或 qr_code）。

**请求示例**

```json
{ "id": 5001 }
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 访客记录 ID |
| `status` | int8 | 签出后状态，固定 `3`（已签出） |
| `checkout_at` | int64 | 签出时间（秒级） |

**错误码**：`M6-E-0001`（未传 id/qr_code）、`M2-E-2002`（访客记录不存在）、`M2-E-5001`

---

### 4.2.4 访客记录分页列表

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/visitors` |
| Handler / Logic | `ListVisitorsHandler` / `ListVisitorsLogic` |

**Query 参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `status` | int8 | 否 | 状态筛选（1/2/3/4），`0`/缺省表示不限 |
| `page` | int64 | 是 | 页码，≥1 |
| `page_size` | int64 | 是 | 每页条数，≥1 |

**响应参数（`data`）**：`total` + `list`（`VisitorItem`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 记录 ID |
| `visitor_name` | string | 访客姓名 |
| `visitor_phone` | string | 手机号 |
| `status` | int8 | 状态（见附录 A.2） |
| `visit_time` | int64 | 预期到访时间（秒级），未设置为 `0` |
| `checkin_at` | int64 | 签入时间，未签入为 `0` |
| `checkout_at` | int64 | 签出时间，未签出为 `0` |
| `device_id` | string | 开门设备 ID，无为空串 |

**业务规则**：强制 `tenant_id` 隔离；排序 `id DESC`。

**错误码**：`M2-E-5001`；参数缺失返回默认 400

---

### 4.2.5 访客详情与门禁轨迹

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/visitor/:id` |
| Handler / Logic | `GetVisitorDetailHandler` / `GetVisitorDetailLogic` |

**路径参数**：`id`（int64，访客记录 ID）

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 访客记录 ID |
| `inviter_id` | int64 | 邀请人 ID |
| `visitor_name` | string | 访客姓名 |
| `visitor_phone` | string | 手机号 |
| `status` | int8 | 状态（见附录 A.2） |
| `visit_time` | int64 | 预期到访时间（秒级），未设置为 `0` |
| `expire_time` | int64 | 二维码过期时间（秒级），`0` 表示未设置 |
| `checkin_at` | int64 | 签入时间（秒级），未签入为 `0` |
| `checkout_at` | int64 | 签出时间（秒级），未签出为 `0` |
| `device_id` | string | 签入开门设备 ID |
| `track` | array | 门禁进出轨迹（按时间正序），项结构见下 |

`track` 项 `VisitorTrackItem`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `action` | string | 动作：`invite`（邀请生成码）/ `checkin_open_door`（签入+门禁开门）/ `checkout`（签出）/ `expired`（过期失效） |
| `time` | int64 | 动作时间（秒级时间戳） |
| `device_id` | string | 开门设备 ID（仅签入且 M1 开门成功时有值） |
| `remark` | string | 动作说明 |

**错误码**：`M2-E-5001`（详情加载失败）；记录不存在返回空详情由前端处理

---

### 4.2.6 访客黑名单新增/更新

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/visitor/blocklist` |
| Handler / Logic | `AddVisitorBlocklistHandler` / `AddVisitorBlocklistLogic` |
| 权限 | 物业客服、园区管理员 |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `visitor_name` | string | 否 | 访客姓名（便于人工核对） |
| `phone` | string | 否 | 手机号（拉黑维度之一） |
| `id_no` | string | 否 | 身份证号（拉黑维度之一，**当前明文存储，加密方案待组长确认**） |
| `reason` | string | 否 | 拉黑原因 |
| `effective_from` | int64 | 否 | 生效起（秒级时间戳），`0`/缺省 = 立即生效 |
| `effective_to` | int64 | 否 | 生效止（秒级时间戳），`0`/缺省 = 永久 |

> `phone` 与 `id_no` **至少提供一个**作为拉黑维度，否则返回 `M6-E-0001`。

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 黑名单记录 ID（新增或命中刷新的 existing ID） |

**业务规则**

1. 同租户下已存在**生效中**且同 `phone` 或同 `id_no` 的记录时，刷新其字段与生效状态（**不重复插入**）；否则新增一行。
2. 状态 `status=1`（生效/禁入）；解除见 [4.2.7](#427-访客黑名单解除)。
3. 实时拦截逻辑见 [4.2.1](#421-发起访客邀请) 与 [4.2.2](#422-访客扫码签入)：邀请与签入阶段均按 `phone`/`id_no` 命中生效黑名单则返回 `M2-E-2004`。
4. 落库表 `visitor_blocklist`（对齐 `deploy/sql/m2_p2_visitor_blocklist.sql`）。

**错误码**：`M6-E-0001`（手机号与身份证均缺失）、`M2-E-5001`（写库/查询失败）

---

### 4.2.7 访客黑名单解除

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/visitor/blocklist/:id/unblock` |
| Handler / Logic | `UnblockVisitorBlocklistHandler` / `UnblockVisitorBlocklistLogic` |
| 权限 | 物业客服、园区管理员 |

**路径参数**：`id`（int64，黑名单记录 ID）

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 黑名单记录 ID |
| `status` | int8 | 解除后状态，固定 `2`（已解除，允许通行） |

**业务规则**

1. 条件更新 `status=1 → 2`，`RowsAffected==0` 表示记录不存在或已解除，返回 `M2-E-5001`（黑名单记录不存在或已解除）。
2. 解除后该人员不再被实时拦截。

**错误码**：`M2-E-5001`（记录不存在或已解除）

---

## 4.3 parking-service（停车服务）

> 停车状态机见[附录 A.3](#a3-停车)。**生产数据的实际来源是 M1 设备 Kafka 事件**，HTTP 入口主要用于联调与人工补录，两条通道走同一段业务逻辑与计费规则。

### 4.3.1 车辆入场

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/parking/entry` |
| Handler / Logic | `ParkingEntryHandler` / `ParkingEntryLogic` |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `plate_no` | string | 是 | 车牌号 |
| `vehicle_type` | int8 | 是 | 1 月卡、2 临时、3 VIP、4 异常；**取值必须为 1~4**（传 0 会按"非月卡/VIP"参与计费） |
| `device_id_in` | string | 是 | 入场设备（地磁/道闸）ID |

**请求示例**

```json
{ "plate_no": "粤B12345", "vehicle_type": 2, "device_id_in": "geomag-in-01" }
```

**响应参数（`data`）**：`ParkingRecordResp`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 停车记录 ID |
| `plate_no` | string | 车牌号 |
| `entry_time` | int64 | 入场时间（秒级） |
| `exit_time` | int64 | 离场时间，入场时为 `0` |
| `duration_min` | int64 | 停车时长（分钟），入场时为 `0` |
| `fee` | string | 费用字符串，入场时为 `""`（未结算） |
| `status` | int8 | 固定 `1`（停车中） |

**业务规则**

1. 每次入场**新建一条**记录（状态 `1` 停车中），同一车牌可存在多条历史记录。
2. 发布 Kafka 事件 `parking-entry`（key = 车牌号）。

**错误码**：`M2-E-5001`（写库失败/内部错误）

---

### 4.3.2 车辆离场（计费）

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/parking/exit` |
| Handler / Logic | `ParkingExitHandler` / `ParkingExitLogic` |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `plate_no` | string | 是 | 车牌号 |
| `device_id_out` | string | 是 | 出场设备（地磁/道闸）ID |

**请求示例**

```json
{ "plate_no": "粤B12345", "device_id_out": "geomag-out-01" }
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 停车记录 ID |
| `plate_no` | string | 车牌号 |
| `entry_time` | int64 | 入场时间（秒级） |
| `exit_time` | int64 | 离场时间（秒级） |
| `duration_min` | int64 | 停车时长（分钟，向下取整） |
| `fee` | string | 应收费用，如 `"15.00"` |
| `status` | int8 | 固定 `2`（已完成） |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": {
    "id": 7001,
    "plate_no": "粤B12345",
    "entry_time": 1757894400,
    "exit_time": 1757901600,
    "duration_min": 120,
    "fee": "10.00",
    "status": 2
  }
}
```

**计费规则（`svc.CalcFee`，当前实现）**

| 车辆类型 | 规则 |
| --- | --- |
| 月卡（1）、VIP（3） | 免费（`0.00`） |
| 临时（2） | 停车 ≤ 15 分钟免费；超出按 **5 元/小时**，**不足 1 小时向上取整** |
| 异常（4） | 按临时车规则计费，并额外发布告警事件 |

> 计费示例：停车 16 分钟 → 1 小时 → 5.00 元；停车 61 分钟 → 2 小时 → 10.00 元。
> 规则表 `parking_fee_rule` 已建表，后续可扩展为可配置计费。

**业务规则**

1. 取该车牌在该园区**最近一条** `status=1` 的记录，无则报错。
2. 更新 `status=2`、`exit_time`、`duration_min`、`fee`、`device_id_out`。
3. 发布 `parking-exit` 事件；若 `vehicle_type=4`（异常车），额外发布 `alarm-event`（`severity=2`）供 M3 安防联动。

**错误码**

| code | HTTP | 场景 |
| --- | --- | --- |
| `M2-E-3001` | 500 | 未找到在场停车记录（车牌无 `status=1` 记录） |
| `M2-E-5001` | 500 | 计费/写库失败等内部错误 |

---

### 4.3.3 停车记录分页列表

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/parking/records` |
| Handler / Logic | `ListParkingHandler` / `ListParkingLogic` |

**Query 参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `status` | int8 | 否 | 1 停车中、2 已完成；`0`/缺省表示不限 |
| `plate_no` | string | 否 | 车牌号**精确**筛选 |
| `page` | int64 | 是 | 页码，≥1 |
| `page_size` | int64 | 是 | 每页条数，≥1 |

**响应参数（`data`）**：`total` + `list`（`ParkingItem`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 记录 ID |
| `plate_no` | string | 车牌号 |
| `entry_time` | int64 | 入场时间（秒级） |
| `exit_time` | int64 | 离场时间，未离场为 `0` |
| `status` | int8 | 1 停车中、2 已完成 |
| `fee` | string | 费用字符串，未结算为 `"0.00"` |

**业务规则**：强制 `tenant_id` 隔离；排序 `id DESC`。

**错误码**：`M2-E-5001`；参数缺失返回默认 400

---

### 4.3.4 在场车辆列表

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/parking/active` |
| Handler / Logic | `ActiveParkingHandler` / `ActiveParkingLogic` |

**Query 参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | int64 | 是 | 页码，≥1 |
| `page_size` | int64 | 是 | 每页条数，≥1 |

**响应参数（`data`）**：与 [4.3.3](#433-停车记录分页列表) 完全一致（`ParkingListResp`），其中所有记录的 `status` 恒为 `1`、`exit_time` 恒为 `0`。

**业务规则**：等价于 `GET /api/parking/records?status=1`，仅返回在场（停车中）车辆。

**错误码**：`M2-E-5001`；参数缺失返回默认 400

---

## 4.4 notice-service（公告服务）

> 公告状态见[附录 A.4](#a4-公告)。

### 4.4.1 发布公告

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/notice` |
| Handler / Logic | `CreateNoticeHandler` / `CreateNoticeLogic` |
| 权限 | 园区管理员、物业客服 |

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | 是 | 公告标题 |
| `content` | string | 是 | 公告正文 |
| `type` | int8 | 是 | 1 通知、2 公告、3 活动、4 停水、5 停电；**取值必须为 1~5** |
| `top` | bool | 否 | 是否置顶，缺省 `false` |
| `publish_at` | int64 | 否 | 定时发布时间（秒级）；`0`/缺省 = **立即发布** |

**请求示例（立即发布）**

```json
{
  "title": "9月18日园区停水通知",
  "content": "因管网检修，9月18日 09:00-17:00 全园区停水，请提前储水。",
  "type": 4,
  "top": true,
  "publish_at": 0
}
```

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 公告 ID |
| `title` | string | 标题 |
| `type` | int8 | 类型 |
| `status` | int8 | `1` 草稿（定时） / `2` 已发布 |
| `top` | bool | 是否置顶 |
| `publish_at` | int64 | 发布时间（秒级）；草稿返回设定的计划时间 |

**响应示例**

```json
{
  "code": "0",
  "msg": "ok",
  "data": { "id": 9001, "title": "9月18日园区停水通知", "type": 4, "status": 2, "top": true, "publish_at": 1757894400 }
}
```

**业务规则**

1. `publish_at = 0` → 状态置 `2` 已发布，`publish_at = now`，并发布 Kafka `notice-event`（key = 标题）。
2. `publish_at > 0` → 状态置 `1` 草稿，由**定时发布调度**（`internal/cron/publish.go`，配置段 `PublishCron`，默认每分钟扫描 + 启动补偿）到期自动置 `2` 并补发 `notice-event`；
   多实例安全：发布动作为 `status=草稿` 条件更新，仅 `RowsAffected==1` 的实例投递事件，不会重复推送。
3. 发布人 `publisher_id` 取自网关 `x-user-id`。

**错误码**：`M6-E-0001`（缺少租户信息）、`M2-E-5001`

---

### 4.4.2 公告分页列表

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/notices` |
| Handler / Logic | `ListNoticesHandler` / `ListNoticesLogic` |

**Query 参数**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `type` | int8 | 否 | 类型筛选，`0`/缺省表示不限 |
| `status` | int8 | 否 | 1 草稿、2 已发布、3 已撤回；`0`/缺省表示不限 |
| `page` | int64 | 是 | 页码，≥1 |
| `page_size` | int64 | 是 | 每页条数，≥1 |

**响应参数（`data`）**：`total` + `list`（`NoticeItem`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 公告 ID |
| `title` | string | 标题 |
| `type` | int8 | 类型 |
| `status` | int8 | 状态 |
| `top` | bool | 是否置顶 |
| `publish_at` | int64 | 发布时间（秒级），未发布为 `0` |
| `created_at` | int64 | 创建时间（秒级） |

**业务规则**：强制 `tenant_id` 隔离；排序 **`top DESC, id DESC`**（置顶优先，其次最新在前）。

**错误码**：`M2-E-5001`；参数缺失返回默认 400

---

### 4.4.3 站内信未读计数

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `GET /api/notices/unread-count` |
| Handler / Logic | `NoticeUnreadCountHandler` / `NoticeUnreadCountLogic` |

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `count` | int64 | `notice_read` 中当前用户 `read_at IS NULL` 的条数（零 SQL 扫描公告表） |

**错误码**：`M6-E-0001`、`M2-E-5001`

---

### 4.4.4 公告已读回填（两个等价路由）

| 项 | 值 |
| --- | --- |
| 方法 / 路径（body 版） | `POST /api/notices/read`，Body `{"notice_id": 9001}` |
| 方法 / 路径（路径版别名） | `POST /api/notice/{id}/read`（无 Body；评审要求的 RESTful 风格别名） |
| Handler / Logic | `MarkNoticeReadHandler` / `MarkNoticeReadByIdHandler` → 同一 `MarkNoticeReadLogic` |

**响应参数（`data`）**：`updated`（本次实际回填条数；对已读/广播型公告重复调用返回 `0`，**幂等**）。

**业务规则**：仅回填当前用户（`x-user-id`）本人的 `notice_read` 记录；记录不存在时不报错、`updated=0`。

**错误码**：`M6-E-0001`、`M2-E-5001`

---

### 4.4.5 公告撤回

| 项 | 值 |
| --- | --- |
| 方法 / 路径 | `POST /api/notice/:id/recall` |
| Handler / Logic | `RecallNoticeHandler` / `RecallNoticeLogic` |
| 权限 | 园区管理员、物业客服（发布人同园区） |

**路径参数**：`id`（int64，公告 ID）

**请求参数（JSON Body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `reason` | string | 否 | 撤回原因（写入 `recall_reason` 审计字段） |

**响应参数（`data`）**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 公告 ID |
| `status` | int8 | 撤回后状态，固定 `3`（已撤回） |

**业务规则**

1. **仅已发布（`2`）可撤回**；草稿（`1`）未发布、已撤回（`3`）不可重复撤回，否则返回 `M2-E-4002`。
2. 更新 `status=3`、`recalled_at=now`、`recall_reason`（审计列，由 `deploy/sql/m2_p2_notice_recall.sql` 追加，向后兼容）。
3. **补偿事件**：撤回成功后重新投递 Kafka `notice-event`（载荷 `status=3`），在线前端据状态将公告横幅/红点下架，保证"撤回即时生效"。
4. 事件投递为**尽力而为**：Kafka 不可用时仅记日志，不影响撤回成功。

**错误码**

| code | HTTP | 场景 |
| --- | --- | --- |
| `M2-E-4001` | 500 | 公告不存在（或不属于当前租户） |
| `M2-E-4002` | 500 | 仅已发布（2）公告可撤回（草稿/已撤回不可撤回） |
| `M2-E-5001` | 500 | 撤回写库/加载失败等内部错误 |

---

## 附录

### 附录 A：枚举字典

#### A.1 工单

| 字段 | 取值 |
| --- | --- |
| `type` 工单类型 | 1 报修、2 投诉、3 巡检、4 保洁、5 装修、6 搬运、7 其他 |
| `priority` 优先级 | 1 紧急、2 普通、3 低 |
| `status` 状态 | 0 待派单、1 处理中、2 待验收、3 已完成、**4 已关闭（终态）** |

**状态流转（FSM，实现于 `workorder-service/internal/state/fsm.go`）**

```text
待派单(0) --assign--> 处理中(1) --submit--> 待验收(2) --approve--> 已完成(3)
                                      \--reject--> 处理中(1)
活跃态(0/1/2/3) --close--> 已关闭(4)
```

#### A.2 访客

| 字段 | 取值 |
| --- | --- |
| `status` | 1 待使用、2 已签入、3 已签出、4 已过期 |
| `verify_channel` 核验方式（P2） | 1 二维码（默认）、2 手机号、3 身份证、4 人脸、5 工牌 |
| `visitor_record.blacklisted` | 0 否、1 是（仅标记本记录是否命中黑名单） |
| `visitor_blocklist.status`（P2） | 1 生效（禁入）、2 解除（允许通行） |

```text
待使用(1) --checkin(核销)--> 已签入(2) --checkout--> 已签出(3)
待使用(1) --超期--> 已过期(4)

多方式核验: 签入按 verify_channel 选择维度(二维码验签 / 手机号 / 身份证 / 人脸 / 工牌)
黑名单实时拦截: 邀请与签入阶段查 visitor_blocklist(生效中且时间窗内), 命中返回 M2-E-2004
```

#### A.3 停车

| 字段 | 取值 |
| --- | --- |
| `vehicle_type` 车辆类型 | 1 月卡、2 临时、3 VIP、4 异常 |
| `status` | 1 停车中、2 已完成 |

```text
停车中(1) --exit(计费)--> 已完成(2)
```

#### A.4 公告

| 字段 | 取值 |
| --- | --- |
| `type` | 1 通知、2 公告、3 活动、4 停水、5 停电 |
| `status` | 1 草稿、2 已发布、3 已撤回（P2 已提供撤回接口 `POST /api/notice/:id/recall`，仅已发布可撤回） |

```text
草稿(1) --到期调度--> 已发布(2) --撤回(recall)--> 已撤回(3)
(publish_at=0 直接) --> 已发布(2)
已撤回(3) 为终态, 不可再流转/重复撤回
```

---

### 附录 B：错误码总表

错误码格式：`{模块}-{级别}-{4 位数字}`；M2 占用前缀 `M2-E-1xxx~5xxx`。

| code | HTTP | 含义 | 触发接口 |
| --- | --- | --- | --- |
| `0` | 200 | 成功 | 全部 |
| `M6-E-0001` | 500 | 请求参数错误（如缺少 `x-tenant-id`、签出未传 id/qr_code） | 建单、邀请、公告、签出 |
| `M2-E-1001` | 500 | 工单不存在 | 派单、状态流转、详情 |
| `M2-E-1002` | 500 | 工单状态非法或流转被禁止 | 派单、状态流转 |
| `M2-E-1003` | 500 | 工单派单/流转并发冲突（乐观锁失败） | 派单、状态流转 |
| `M2-E-2001` | 500 | 访客二维码已过期 | 签入 |
| `M2-E-2002` | 500 | 访客二维码无效或已被核销 | 签入、签出 |
| `M2-E-2003` | 500 | 访客签入失败 | 签入 |
| `M2-E-2004` | 500 | 访客被拉黑，禁止通行（邀请/签入实时拦截） | 邀请、签入 |
| `M2-E-2005` | 500 | 不支持的核验方式或凭证缺失/不匹配（多方式核验） | 签入 |
| `M2-E-3001` | 500 | 停车记录不存在（无在场记录） | 离场 |
| `M2-E-3002` | 500 | 停车计费失败（已定义，暂未单独抛出） | 离场 |
| `M2-E-3003` | 500 | 停车设备不存在或未接入（已定义，暂未单独抛出） | 入场/离场 |
| `M2-E-4001` | 500 | 公告不存在（或不属于当前租户） | 撤回 |
| `M2-E-4002` | 500 | 仅已发布（2）公告可撤回（草稿/已撤回不可撤回） | 撤回 |
| `M2-E-5001` | 500 | M2 服务内部错误兜底（含 DB 未初始化） | 全部 |
| `M6-E-0004` | 404 | 网关未匹配路由前缀 | 网关 |
| `M6-E-0006` | 502 | 网关上游服务不可用 | 网关 |

> 错误码定义见 `common/errorx/codes.go`，HTTP 映射见 `common/errorx/http_status.go`。

---

### 附录 C：异步事件契约（Kafka）

M2 部分能力为**事件驱动**，非 HTTP 调用，对接方（M5 大屏、M3 安防、通知类服务）按主题订阅。

| 主题 | 生产方 | 消息 key | 触发时机 | 载荷要点 |
| --- | --- | --- | --- | --- |
| `workorder-event` | workorder-service | 工单号 | 建单/派单/状态流转 | `event_id`(取自 `x-request-id`)、`event`(created/assigned/status_changed)、`action`、`tenant_id`、`work_order_id`、`order_no`、`from_status`、`to_status`、`operator_id`、`assignee_id`、`timestamp` |
| `parking-entry` | parking-service | 车牌号 | 车辆入场 | `ParkingRecord` 序列化 |
| `parking-exit` | parking-service | 车牌号 | 车辆离场 | `ParkingRecord` 序列化 |
| `alarm-event` | parking-service | 车牌号 | 异常车离场 | `device_id`、`severity`、`content`、`timestamp` |
| `notice-event` | notice-service | 公告标题 | 公告发布成功（含定时发布调度到期发布）；**撤回时重投**（载荷 `status=3`，供在线前端即时下架） | `Notice` 序列化 |
| `visitor-event` | visitor-service | 访客记录 ID | 邀请签发 / 扫码签入 / 签出离场 | `event_id`(取自 `x-request-id`)、`event`(invite/checkin/checkout)、`tenant_id`、`visitor_id`、`visitor_name`、`visitor_phone`、`inviter_id`、`status`、`device_id`、`timestamp` |
| `device-telemetry` | M1 event-dispatcher（**消费方**为 parking-service） | 设备 ID | 地磁/门禁上报 | 标准信封 `request_id`/`event_type`/`occurred_at` + `payload{tenant_id,plate_no,vehicle_type}`；兼容旧扁平格式；**入场按 `request_id` 幂等**（`parking_record.uk_request` 去重） |

**投递语义**

- M2 生产侧均为**尽力而为**：Kafka 不可用时仅记日志，**不回滚、不阻断**主业务（HTTP 仍返回成功）。
- 消费侧（停车）对格式非法/未知 `event` 的消息记 warn 后跳过，不阻塞后续消费；重复消费按业务键幂等处理。
- 主题与消费组常量定义于 `common/kafka/topics.go`。

---

### 附录 D：联调示例（curl）

```bash
# 0) 统一约定: 经网关 8080, 携带园区上下文头
BASE=http://localhost:8080

# 1) 创建工单
curl -i -X POST $BASE/api/workorder -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 3001" \
  -d '{"type":1,"title":"水管漏水","description":"厨房渗水","priority":1,"location":"3号楼502"}'

# 2) 派单 (工单ID=1001)
curl -i -X PUT $BASE/api/workorder/1001/assign -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 2000" \
  -d '{"assignee_id":2001,"department_id":12}'

# 3) 状态流转: 提交 -> 验收
curl -i -X PUT $BASE/api/workorder/1001/status -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"action":"submit"}'
curl -i -X PUT $BASE/api/workorder/1001/status -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"action":"approve","remark":"验收通过"}'

# 4) 工单列表 / 详情
curl -i "$BASE/api/workorders?status=1&page=1&page_size=10" -H "x-tenant-id: 1"
curl -i "$BASE/api/workorder/1001" -H "x-tenant-id: 1"

# 5) 访客邀请 -> 签入 -> 签出
curl -i -X POST $BASE/api/visitor/invite -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 3001" \
  -d '{"visitor_name":"张三","visitor_phone":"13800000000","visit_time":1757905200,"expire_time":1757991600,"reason":"探亲"}'
curl -i -X POST $BASE/api/visitor/checkin -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"qr_code":"<上一步返回的 qr_code>"}'
curl -i -X POST $BASE/api/visitor/checkout -H "Content-Type: application/json" -H "x-tenant-id: 1" -d '{"id":5001}'

# 6) 停车入场 -> 离场 -> 在场列表
curl -i -X POST $BASE/api/parking/entry -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"plate_no":"粤B12345","vehicle_type":2,"device_id_in":"geomag-in-01"}'
curl -i -X POST $BASE/api/parking/exit -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"plate_no":"粤B12345","device_id_out":"geomag-out-01"}'
curl -i "$BASE/api/parking/active?page=1&page_size=10" -H "x-tenant-id: 1"

# 7) 公告发布 -> 列表 -> 撤回 -> 已读回填
curl -i -X POST $BASE/api/notice -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 2000" \
  -d '{"title":"停水通知","content":"9月18日停水","type":4,"top":true,"publish_at":0}'
curl -i "$BASE/api/notices?page=1&page_size=10" -H "x-tenant-id: 1"
# 7.1) 公告撤回(P2): 仅已发布可撤回, 置 status=3 + 重投 notice-event
curl -i -X POST $BASE/api/notice/9001/recall -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 2000" \
  -d '{"reason":"信息有误, 重新发布"}'
# 7.2) 站内信未读计数 / 已读回填(P2)
curl -i "$BASE/api/notices/unread-count" -H "x-tenant-id: 1" -H "x-user-id: 3001"
curl -i -X POST $BASE/api/notices/read -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 3001" \
  -d '{"notice_id":9001}'

# 8) 访客黑名单(P2): 拉黑手机号 -> 邀请被拦截 -> 解除
curl -i -X POST $BASE/api/visitor/blocklist -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 2000" \
  -d '{"visitor_name":"李四","phone":"13800000099","reason":"违规来访","effective_from":0,"effective_to":0}'
curl -i -X POST $BASE/api/visitor/invite -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 3001" \
  -d '{"visitor_name":"李四","visitor_phone":"13800000099","visit_time":1757905200,"expire_time":1757991600}'
# 预期返回 M2-E-2004(访客已被拉黑, 禁止邀请)
curl -i -X POST $BASE/api/visitor/blocklist/6001/unblock -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 2000"

# 9) 访客多方式核验(P2): 身份证方式邀请 + 身份证方式签入
curl -i -X POST $BASE/api/visitor/invite -H "Content-Type: application/json" -H "x-tenant-id: 1" -H "x-user-id: 3001" \
  -d '{"visitor_name":"王五","visitor_phone":"13800000111","visit_time":1757905200,"expire_time":1757991600,"verify_channel":3,"id_no":"11010119900307XXXX"}'
curl -i -X POST $BASE/api/visitor/checkin -H "Content-Type: application/json" -H "x-tenant-id: 1" \
  -d '{"verify_channel":3,"credential":"11010119900307XXXX"}'
```

---

### 附录 E：已知限制与注意事项

| # | 事项 | 影响 | 建议 |
| --- | --- | --- | --- |
| 1 | 业务错误码均为 `-E-` 级 → HTTP 500 | 前端若按 HTTP 状态码判定会误判为服务异常 | **一律以响应体 `code` 判定**；后续可改用 `-W-` 级返回 400 |
| 2 | 参数校验失败走 go-zero 默认错误体 | 该分支**不是**统一信封 | 前端对 400 做兼容解析 |
| 3 | 数据库未就绪时列表接口可能空指针 panic | 返回 500（非 `M2-E-5001`） | 加 nil-DB 守卫返回 `M2-E-5001`（**待修复**：`listworkorderslogic.go:35` 等） |
| 4 | 网关当前**无鉴权**，`x-tenant-id` 缺省为 `1` | 联调数据落在默认园区；无越权防护 | 接入 M6 `ValidateToken/CheckPermission` 后由鉴权中间件覆盖 |
| 5 | M1 `device-service` 尚无 gRPC server 实现 | 访客签入必然走降级，`device_id` 为空 | 属预期行为；M1 就绪后自动回填真实设备 ID |
| 6 | Kafka 事件"尽力而为" | 极端情况下可能丢事件（业务成功但事件未达） | 对账类场景以 DB 为准，不依赖事件做账 |
| 7 | 计费规则为代码内固定规则（5 元/小时、15 分钟免费） | 无法在线调整费率 | `parking_fee_rule` 表已就绪，可改为可配置 |
| 8 | 公告"定时发布"依赖调度任务 | `publish_at > 0` 的公告由 `PublishCron` 调度到期自动发布（2026-09-18 已实现，默认每分钟 + 启动补偿，多实例条件更新防重） | 调度精度为分钟级 |

---

### 附录 F：契约文件索引

| 契约 | 路径 |
| --- | --- |
| 工单 HTTP | `app/workorder-service/workorder.api` |
| 访客 HTTP | `app/visitor-service/visitor.api` |
| 停车 HTTP | `app/parking-service/parking.api` |
| 公告 HTTP | `app/notice-service/notice.api` |
| 工单 gRPC | `proto/workorder/workorder.proto` |
| 设备 gRPC（M1，被 M2 调用） | `proto/device/device.proto` |
| 统一响应体 | `common/response/response.go` |
| 错误码 | `common/errorx/codes.go`、`common/errorx/http_status.go` |
| Kafka 主题 | `common/kafka/topics.go` |
| 网关路由表 | `gateway/etc/apigateway-api.yaml` |

---

## 变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| V1.0 | 2026-09-15 | 首版：M2 全部 16 个接口（15 HTTP + 1 gRPC）定义、统一约定、枚举字典、错误码表、Kafka 事件契约、联调示例与已知限制 |
| V1.1 | 2026-09-18 | 增补：公告未读计数/已读回填（body 版 + `POST /api/notice/:id/read` 路径版别名）、定时发布调度说明、`visitor-event` 事件契约、遥测入场 request_id 幂等口径；配套《M2业务场景说明.md》发布 |
| V1.2 | 2026-09-20 | P2 三功能入库文档化：① 公告撤回 `POST /api/notice/:id/recall`（已发布→已撤回 + `recalled_at`/`recall_reason` 审计列 + notice-event 补偿重投，错误码 M2-E-4001/4002）；② 访客黑名单 `POST /api/visitor/blocklist`（新增/刷新）、`POST /api/visitor/blocklist/:id/unblock`（解除），邀请/签入实时拦截（M2-E-2004）；③ 访客多方式核验 `verify_channel` 1二维码/2手机号/3身份证/4人脸/5工牌，签入按维度凭证核验（M2-E-2005）；另补 4.2.5 访客详情+门禁轨迹。接口总数 16→23（22 HTTP + 1 gRPC）。注：`id_no`/`face_token` 当前明文存储，加密方案待组长确认 |
