# M5 未实现清单（全量核查）

| 项 | 内容 |
|---|---|
| 核查对象 | M5 运营招商 + 指挥调度（`leasing` / `dashboard` / `dispatch`） |
| 核查日期 | 2026-09-20 |
| 核查方式 | **逐条读代码与 DDL 取证**，不凭印象；含"撤回误判"一节（避免虚报） |
| 结论 | 🔴 功能缺口 **10 项** ｜ 🟡 质量欠账 **4 项** ｜ 🟢 可清理 **1 项** ｜ ✅ 核查后**不是**缺口的 **5 项** |
| 修复进度 | **A 组已闭环**（A1~A4 / A6~A8 / A10 已修；A5 确认不做）｜ **B 组已全部闭环**（B1~B4）｜ C1 待前端确认 —— 明细见各节 |

---

## ✅ 本轮（2026-09-20）已修复

| # | 缺口 | 修复内容 | 验证证据 |
|---|---|---|---|
| **A2** | 账单查不到 | `GET /api/lease/bills`（按合同/账期/状态/园区筛选 + 分页） | 端到端：`total=5`，金额返回定长 `"15000.00"` |
| **A3** | 账单无汇总 | `GET /api/lease/bills/summary`（应收/已收/欠费，**一条 SQL 出 6 个数**） | 缴费后 `unpaid 10000 + paid 15000 = total 25000` ✅ 账目自洽 |
| **A1** | 账单缴不了费 | `PUT /api/lease/bill/:id/status`（`action=pay\|unpay`） | 重复 `pay` 返回同一状态（幂等）；非法 action 被拒 |
| **A4** | `lease_zone` 无写入路径 | `POST /api/lease/zone`（upsert）+ `GET /api/lease/zones` | 同编码二次提交返回**同一 id**（更新非新增）；入驻率分母 3500 → **10500** |
| **A6** | 缓存击穿无防护 | `overviewlogic` 引入 `singleflight` + 结果浅拷贝 | `TestOverview_StampedeProtection`：50 并发、上游**只被调用 1 次**（耗时 0.20s = 桩阻塞时长） |

**修复带来的测试变化**：`leasing/internal/logic/lease` 此前 **0 个测试文件**，现新增 2 个 DB 用例（账单闭环 + 区域维护）。
全量：**PASS 55 / FAIL 0 / SKIP 1**（唯一跳过的是需环境变量门控的 `TestExternalGRPC`）。

### ⚠️ 修复过程中踩到两个坑（值得记）

1. **`replace_in_file` 报成功但内容未落盘**：往 `.api` 里插类型定义时，工具报 `0 lines removed. 76 lines added`，实际文件没变（goctl 随即报 `type BillListReq not defined`）。**怀疑是 `new_str` 里的 emoji（⚠️）与注释内双引号所致** —— 去掉后一次成功。
   → 对策：改完关键位置**立刻 read/grep 复核**，不要因为"工具说成功"就往下走。

2. **⚠️ SKIP 会让测试套件"说谎"**：本机 Docker 未启动时，`openTestDB` 走 `t.Skip`，而 **`t.Skip` 在 `go test` 里算 PASS** —— 套件照样打印一行 `ok`。
   差点把"账单测试全过"当成真实证据上报，实际是 **`--- SKIP: TestBillFlow 跳过: MySQL 不可用`**。
   → 对策：跑测试时**统计 `--- SKIP` 条数**，不能只看 `ok`。已在最终验证里加了这个统计。

---

## 🔴 A. 功能缺口（缺接口 / 缺能力 —— 不做就是缺）

| # | 缺口 | 证据（可直接核对） | 影响 |
|---|---|---|---|
| ~~A1~~ | ~~账单缴不了费~~ | ✅ **已修复**（`PUT /bill/:id/status`） | — |
| ~~A2~~ | ~~账单查不到~~ | ✅ **已修复**（`GET /bills`） | — |
| ~~A3~~ | ~~账单无汇总~~ | ✅ **已修复**（`GET /bills/summary`） | — |
| ~~A4~~ | ~~`lease_zone` 无维护接口~~ | ✅ **已修复**（`POST /zone` + `GET /zones`） | — |
| **A5** | ✔️ **不做** —— `dispatch` 无租户维度（**本期确认为单园区**） | ① `dispatch_task` 无 `tenant_id` ② `dispatch_staff` 无 `tenant_id` ③ 各请求 DTO 无租户参数 | 见下方「A5 决策记录」 |
| ~~A6~~ | ~~缓存击穿无防护~~ | ✅ **已修复**（`singleflight` + 结果浅拷贝） | — |
| **A7** | ~~`healthz` / `readyz` 未接入~~ | ✅ **已完成**（2026-09-21 复查发现）：`common/health.Handler` 就绪，**全平台 14+ 服务都已接入**，M5 三服务的 `routes.go` 里也已有 `GET /health`（develop 铺开时一并加的，随合并进来）| 原记录「等 M6」**已过时**；`deploy/m5` 的探测已从 TCP 升级为真 HTTP 探测 |
| **A8** | **Docker Compose 未含 M5** | ✅ **M5 片段已出并实测通过**（`deploy/m5/`：4 服务全 healthy，6 个接口实测 200）<br>⚠️ 但**「全平台一键启动」仍未达成** —— 主 compose 依旧只有中间件，全仓只有 `deploy/m5` 有 Dockerfile | 计划书验收标准需**平台负责人合并各模块片段**后才能给结论；M5 这半边已交付 |
| **A9** | ~~WebSocket 鉴权未启用~~ | ✅ **主体已完成**（2026-09-22 复查：随 `fix(M1/M3/M5)` 那次 P0 修复进来的，**连老弟原先「钉住未校验」的用例都被同步改写成了 `TestHandler_RejectsBadTokens`**）。现状：`ws.go` 里 `?token=` **已真校验**（JWT 签名/过期/**access 类型**，refresh 令牌拒绝接入；`jwtSecret` 未配置时**拒绝所有连接**；网关注入身份优先）；缺 token / 垃圾串 / 异密钥 / refresh 均 **401**<br>⚠️ **仍开放一项**：`CheckOrigin` 仍是 `return true`（源码注释自认「开发期放开，**上线前必须收敛为域名白名单**」）→ 跨站 WebSocket 劫持面仍在 | 只差 `CheckOrigin` 白名单，属**上线前收敛项**（与 M6 统一鉴权口径一起做） |
| **A10** | ~~Kafka 端到端未验证~~ | ✅ **已完成**（2026-09-21）：本地 broker 端到端 **PASS=45 / FAIL=0**，脚本退出码 0<br>覆盖：投告警→自动建单（含审计 `0→1`）、完整业务闭环（审计序列 `create,assign,assign,release,assign,start,finish,close`）、**双实例并发只落 1 单**、**毒消息不阻塞分区**<br>详见 `docs/m5/07-Kafka端到端验证报告.md` | ⚠️ **仅剩**「连组长**共享** broker」未验证（依赖 topic 授权与网络可达，非 M5 可控）；代码路径与本地完全一致 |

---

## 🟡 B. 质量欠账

| # | 项 | 证据 | 影响 |
|---|---|---|---|
| **B1** | ⭐⭐ **业务主干零测试** | ✅ **部分已补**（2026-09-20）：`leasing/logic` 已建测试；`dispatch/logic` 仍为 0 | — |
| **B2** | **覆盖率未达计划书线** | ✅ **已大幅改善**（见下方「本轮补测」），最弱项从 5.5% 提到 44.0% | 仅剩 `wsserver 44.0%` / `leasing/logic 47.5%` 未过半 |
| **B3** | **合同入参校验无测试** | `contractcreatelogic` 校验很完整（负数金额/押金、日期倒置、`auto_renew` 取值、提醒天数越界都拦），但**0 用例** | 有人误删一条校验**不会有测试报警** |
| **B4** | ~~契约断言未成体系~~ | ✅ **已完成**（2026-09-22），**并撤回原判断** —— 「恒等式断言」其实**早已存在**：`alarm_test.go:94` 与 `device_test.go:88` 都断言了分项之和 == total（后者注释还写明"上游新增状态码时本断言会失败"）。本轮真正补的是三个**新发现**的缺口：<br>① ⭐ **兑现注释承诺** —— `device.go` 写着「透传 **+ 日志留痕**」，但 `provider` 包**一行 `logx` 都没有**：注释承诺的行为**不存在**，数字对不上时生产环境**零痕迹**（典型假注释）<br>② **完成率值域防线** —— `completion_rate` 越界（如 M2 口径从百分比改成小数）会显示「完成率 150%」而无人认领<br>③ **漂移可检测性断言** —— 证明「上游新增等级码/状态码」时，差额能**从返回值本身**算出来（即适配器内部日志的触发条件）<br>产出：`provider/contract.go` + `provider/contract_test.go`（4 用例）；`provider` 覆盖率 **86.7% → 92.5%** | 已闭环。做法仍守项目原则：**数据原样透传、不本地纠正，只让漂移可见** |

---

## ✅ 本轮补测（2026-09-20 第二轮）

### 覆盖率前后对比

| 包 | 补测前 | 补测后 |
|---|---|---|
| `leasing/internal/state` | **零测试文件** | **100.0%** |
| `dispatch/internal/state` | **零测试文件** | **100.0%** |
| `dispatch/internal/logic/dispatch` | **零测试文件**（8 个源文件） | **55.9%** |
| `dashboard/internal/provider` | 28.9% | **86.7%** |
| `dashboard/internal/wsserver` | 44.0% | **67.9%** |
| `leasing/internal/logic/lease` | 23.1% | **47.5%** |

**M5 全量：PASS 94 / FAIL 0 / SKIP 2**（跳过的是需 `M5_GRPC_ADDR` 的 `TestExternalGRPC` 与「候选池为空」这一开发库构造不出的分支），13 个测试包，连跑两次结果一致。

> **2026-09-21 收口：13 个包覆盖率已全部达标（≥54.7%）**，计划书「核心业务逻辑 ≥ 50%」的硬线首次全包通过。
> 关键一步是删掉 `dispatch/cron` 里一段**从未被实例化过的** `ExpireScanner`（`expire.go`，详见下表），
> 该包覆盖率随之从 45.7 升到 **76.5**。

### 补测覆盖的内容

| 新增测试 | 锁住什么 |
|---|---|
| `dispatch/internal/state/fsm_test.go` | 工单 FSM **整张转移表**（合法 + 非法 + 终态无出边）。`ActionRelease` 是刚加的出边，没有测试谁也不知道被谁删了 |
| `leasing/internal/state/fsm_test.go` | 合同 FSM 整表；并**刻意钉住**「已到期不是终态、已终止才是」这个与工单 FSM 不同的语义 |
| `leasing/internal/logic/lease/contractvalidate_test.go` | 合同**入参校验 13 条分支** + 状态流转主干（创建→生效→续签→终止→终态拒绝）+ 审计流水核对 → **抓到续签不留审计的 Bug** |
| `dashboard/internal/provider/workorder_test.go` | M2 工单适配器（此前 alarm/device/energy 三个都有测试，**唯独工单没有**）+ 4 个未配置占位 + `countCreatedToday` 日期边界 |
| `dispatch/internal/logic/dispatch/taskflow_test.go` | 建单校验（含**伪造告警来源**被拒 + 技能归一 + source 强制人工）、状态流转非法转移、**完成时间只在终态写**、**乐观锁并发只成功一次**、人名三级回退链 |
| `dispatch/internal/logic/dispatch/convert_staff_test.go` | `clampTaskPage` 分页钳制（**不钳制会被 pageSize=100000 扫全表**）、DTO 可空字段不退化、人员 upsert **幂等性**与技能归一、自动指派主路径 |
| `dashboard/internal/wsserver/ws_test.go` | WebSocket **接入 → 注册 → 收到广播 → 断开回落**（演示路径）；并**钉住**「`?token=` 当前未校验」这一已知缺口 |

### ⭐ 补测过程中抓到一个真 Bug：续签不留审计

写 `TestContractLifecycle` 时断言「创建/生效/续签/终止 各一条审计流水」，结果**只有 3 条，`renew` 那条没有**。

根因在 `ContractUpdate`：

```go
if nextStatus != contract.Status {   // 只认"状态变化"
    写审计
}
```

**续签是「生效中 → 生效中」**（改了租期与租金，但不改状态）→ 按状态变化判断，**人工续签完全不留下痕迹**。

而同一件事由定时任务自动续约时**是写审计的**（`cron/daily.go` 显式写 `from=2 to=2`）→
**两条路径两套标准**，违反架构一致性铁律。

**已修**：审计条件改为「状态变化 **或** 合同条款实质变更（`renew` / `update`）」，
并顺手消掉了 `case "update":` 的魔法字符串（提为 `actionUpdate` 常量）。

> 这类 Bug 的特点：**不报错、不崩溃、数据也"看起来对"** —— 只是想查"这合同什么时候续过、租金从多少调到多少"时查不到。
> 只有写了断言审计条数的测试才会发现。

---

## 🔵 2026-09-21 复查新增：合并 develop 后暴露的 2 个租户缺口（已修）

平台侧在 09-16~09-18 落地了 RBAC 数据隔离（`l2_tenant_id_migration.sql` 给业务表统一加 `tenant_id`，
鉴权收口到网关 + `IdentityFromHeader` 把 `x-tenant-id` 提升进 ctx）。
M5 随合并接入后，**写入路径没跟上**，暴露出两处缺口：

### 缺口 1：账单三接口的租户越权（**老弟自己 9-20 写的代码**）

| 接口 | 修复前的写法 | 后果 |
|---|---|---|
| `GET /bills` | 用**请求体**的 `req.TenantId`，且 `!= 0` 才过滤 | 传别人园区 id 能看别人账单；**传 0 直接看全部园区** |
| `GET /bills/summary` | 同上 | 同上（金额汇总泄露） |
| `PUT /bill/:id/status` | **完全没有任何租户检查** | 可跨园区给别人账单**缴费/撤销** |

对比：`contractlist/detail/status/occupancy` 全部用 `ctxdata.GetTenantId(ctx)`（网关注入、不可伪造）。

**已修**：三个接口统一改为「只认 ctx 的租户，且始终过滤」；缴费的条件更新也带上租户条件；
不存在与「不属于本租户」返回同一个 NotFound，不泄露「该账单存在」。
并在 `TestBillFlow` 补了**跨租户断言**（另一个租户：列表为空、汇总为 0、缴费被拒且**状态未被改动**）。

### 缺口 2：dispatch 建单不写租户，但查询按租户过滤

- 迁移给 `dispatch_task` 加了 `tenant_id`；`tasklist/detail/status` 都按 ctx 的租户过滤（平台侧改的）
- 但 `taskcreatelogic` 与 `consumer` **都没写这个字段** → 恒为 DB 默认值 0
- **后果**：网关注入非 0 租户（平台口径：默认园区 = 1）时，**用户建完单立刻在列表里看不到自己的单**

**已修**（与 M2/M4 同一约定）：
- HTTP 建单：写 `ctxdata.GetTenantId(ctx)`（谁建的归谁）
- 消费者自动建单：写 `Kafka.DefaultTenantId`（告警消息体不带租户，只能落兜底园区；
  该配置**平台早已存在**且默认 1，只是此前没人用）—— 已把它显式写进 `etc/dispatch-api.yaml`
- 测试补断言：自动建单必须落到兜底园区（用一个非 0 且非 DB 默认值的租户，证明是「配置注入」而非巧合）

### ⚠️ 遗留待确认：**存量数据的租户回填**

迁移脚本只加列（默认 0），**没有回填**。而平台的口径是「默认园区 = 1」。
若网关注入的 `x-tenant-id` 为 1，则**现有 M5 数据（tenant_id=0）会全部查不到**
（工单、合同、账单都按 ctx 租户过滤）。

→ 需确认两件事：① 网关注入的租户到底解析成 0 还是 1；② 存量数据是否回填、回填成几。
迁移脚本原文也提示了这一点：「回填完成前 datascope 默认 ScopeTenant 会过滤真实数据」。

---

## 🧹 2026-09-21：删除一段从未启用过的重复机制

排查 `dispatch/cron` 覆盖率为何偏低时，发现 `expire.go` 里的 `ExpireScanner`：

| 问题 | 证据 |
|---|---|
| **从没运行过** | 全仓搜 `NewExpireScanner` → 除自身定义外**零引用**；`cron.Start` 也只启动了重派任务 |
| **与现有机制职责重合** | 它扫的条件（`status=已指派 AND assign_expire_at < now`）与 `reassign.go` **完全相同** —— 两套机制干同一件事，谁赢了取决于乐观锁 |
| **绕过状态机** | 它在一个事务里直接 `Updates(map[string]interface{}{"status": ...})`，违反「状态流转必须过 `state.Next`」的项目铁律 |
| **拖低覆盖率** | 该包 45.7% → 删除后 **76.5%** |

连带删除 `dispatch/state` 的 `ActionExpire`（只被这段死代码引用，且**从未进转移表** ——
`Next(已指派, expire)` 返回非法），避免后人以为存在第二条通往「待指派」的边。

> ⚠️ **`leasing/state` 也有一个同名的 `ActionExpire`，那是活代码**（生效中 → 已到期，
> `cron/daily.go` 与 `contractupdate` 都在用），**未动**。两个同名常量分属不同包、含义不同，删之前必须查清引用面。

**顺带修掉一个测试干扰**：`RunReassignOnce` 加锁后**不自己解锁**（靠 TTL 过期），每个调用方都要收尾。
既有用例的 `t.Cleanup` 里就有一句 `_ = rdb.Del(ctx, reassignLockKey)`；
新写的三个用例起初漏了这句，把后续用例的扫描挡在门外（表现为 `Reassigned=0` 且无审计，极难定位）。已补齐，连跑三次结果一致。

---

## 🟢 C. 可清理

| # | 项 | 说明 |
|---|---|---|
| **C1** | `/api/dashboard/home` 僵尸接口 | 早期单源接口（仅 M2 工单卡片），已被 `/overview` 完全取代，但**仍在 `routes.go` 里注册** → 保留兼容换来长期维护成本。建议明确"删"或"标注 deprecated" |

---

## ✅ D. 核查后**不是**缺口（撤回误判，避免虚报）

核查过程中先怀疑、读码后**排除**的 5 项：

| 曾怀疑 | 实际结论 | 证据 |
|---|---|---|
| 到期提醒没用上合同自己的 `renew_notice_days` | ✅ **已用上** | `contractexpiringlogic.go:110-111`：`NeedNotice: daysLeft <= c.RenewNoticeDays`，注释写明"进入该合同自己的提醒窗口（而非请求的 days）" |
| 工单 `finish` 没写 `finished_at` | ✅ **已写** | `taskstatuslogic.go:63`：`updates["finished_at"] = time.Now()` |
| 合同入参校验不完整 | ✅ **已完整** | 负数金额/押金、日期倒置、区域空、`auto_renew` 枚举、提醒天数 0~365 全部拦截 |
| 设备卡片恒等式没加断言测试 | ✅ **已有** | `TestDeviceStat_StatusMapping`（1263 = 1240+12+11）+ 不一致时透传用例 |
| M5 代码里有 TODO/占位实现 | ✅ **无**（`ws.go` 的 `TODO(M6)` 是**有意的待办标记**，非未实现） | 三服务全量搜 `TODO/FIXME/未实现/占位` → 仅命中该标记 |

---

## 🧰 顺带发现的工具链事实（影响 Day 3 覆盖率）

```
go test -cover ./app/leasing-service/... -cover -count=1
  → go: no such tool "covdata"
```

**跨包含"无测试文件的包"时会报此错**（本地 Go 1.24.5 与自动下载的 `toolchain@go1.26.0` 混用所致）。
`go tool covdata` 本身存在（缺子命令参数）。**单包 `-coverprofile` 正常可用**：

```powershell
go test ./app/dispatch-service/internal/assign/... -coverprofile=cov.out -count=1   # ✅ 可用
```

→ Day 3 的做法：**逐包测覆盖率再人工汇总**，不要指望一条 `./...` 命令出总覆盖率。

---

## 与「国庆五天计划」的对应关系

这份清单就是那 5 天要清掉的账：

| 清单项 | 落在哪天 |
|---|---|
| A2 A3 A1 | Day 2（账单查询 → 汇总 → 缴费） |
| B1 B2 B3 B4 | Day 3（覆盖率 + 契约断言 + 校验单测） |
| A6 | Day 4（缓存击穿防护） |
| A8 | Day 5（Compose 片段） |
| A10 | Day 1（本地 broker 端到端） |
| A4 A5 A7 A9 C1 | **待定** —— 见下 |

### 决策记录

| 项 | 结论 | 依据 |
|---|---|---|
| **A5 dispatch 无租户维度** | ✔️ **不做** | **本期为单园区部署**（2026-09-20 确认）。详见下方「A5 决策记录」 |
| **A4 `lease_zone` 维护接口** | ✅ 已做 | 2026-09-20 实现 `POST /zone` |
| **A7 healthz/readyz** | ⏳ 等 M6 | 依赖 `common/checks`，M5 只能被动接入 |
| **C1 `/home` 僵尸接口** | ⏳ 待确认 | 删除是破坏性变更，需确认前端是否还有人调 |

---

## A5 决策记录：`dispatch` 不加租户维度（本期单园区）

**结论**：`dispatch_task` / `dispatch_staff` **不加 `tenant_id`**，各接口 DTO 也不加租户参数。

### 为什么

| 理由 | 说明 |
|---|---|
| **本期是单园区部署** | 加租户字段会引入一个**永远是常量**的列，属于典型的过度设计（YAGNI） |
| 加它的连带成本不低 | 要改**建的 2 张表 + 所有请求 DTO + 派单候选池过滤 + 列表筛选 + 缓存**，还要为"告警事件不带 tenant_id"专门做兜底（见下） |
| **告警事件本身没有租户信息** | `alarm-event` 的消息体只有 `device_id` / `event_type` 等。若 dispatch 要按租户隔离，**自动建单就必须猜一个租户** —— M2 的 `workorder-service` 就是这么干的（配置项 `DefaultTenantId: 1` 作为"告警未携带租户时的建单兜底园区"）。单园区下完全不需要这份复杂度 |

### ⚠️ 这一条要写清楚的原因（防止后人"误修"）

`leasing` 有 `tenant_id` + `idx_tenant`，`dashboard` 有租户维度缓存与 `AlarmProvider.Stat(ctx, tenantId)` ——
**唯独 `dispatch` 没有**。这个不对称是**有意的**，不是漏做。

**将来若要支持多园区**，改动清单是：

1. `dispatch_task` / `dispatch_staff` 各加 `tenant_id` + 索引（DDL 在 `deploy/sql/m5_mysql_tables.sql`）
2. `DispatchTask` / `StaffItem` 与 4 个请求 DTO 加字段（改 `.api` 后 `goctl` 重新生成）
3. 列表查询加租户过滤；`assign.Pool()` 的候选池按租户过滤（**否则会把 A 园区的人派去 B 园区**）
4. 自动建单需要一个"告警未携带租户时的兜底园区"配置（参考 M2 的 `DefaultTenantId`）
5. 调度人员维护接口要按租户校验归属

> `leasing` / `dashboard` 已有的租户字段**保持不动** —— 它们是数据隔离列、且当前退化为常量，成本为零；
> 为了"对称"去删它们反而是无谓的破坏性改动。
