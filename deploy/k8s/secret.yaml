apiVersion: v1
kind: Secret
metadata:
  name: onepark-secrets
  namespace: onepark
type: Opaque
# 以下为占位值(base64("change-me")), 部署前务必用真实凭据替换:
#   echo -n '<real>' | base64
data:
  REDIS_PASS: "Y2hhbmdlLW1l"
  AUTH_SECRET: "Y2hhbmdlLW1l"
  # 各 MySQL 密码等按需追加为 data 项(键名与各服务 etc yaml 的 ${...} 对齐).
