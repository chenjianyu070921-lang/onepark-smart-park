# OnePark 智慧园区・进度盘点与单日任务分配（2026-09-18）

**盘点日期**：2026-09-18（周五）
**盘点方式**：git diff 近 5 次提交 + 全量 go build 复测 + GORM 表名提取 + 路由/gRPC/新模块代码审查
**代码增量**：近 24h 合并 5 个 PR，165 文件变更，+11427/-597 行

---

## 第一部分：六人六模块进度盘点表

| 负责人 | 模块 | 完成度 | 已完成内容 | 缺失缺口 |
|---|---|---|---|---|
| **陈建羽**<br>M1 物联接入 | device / shadow / event-dispatcher / gateway | **80%** | 17 服务全 build 中 device/shadow/event-dispatcher/gateway 均通过；HTTP 10 接口 + gRPC 4 方法完整；shadow 带乐观锁；event-dispatcher MQTT 解析+双 topic 投递+单测；gateway TCP 认证。今日仅 device.go 小改 5 行。 | device 表缺 type/lat/lng 列 TODO 仍在；event-dispatcher 与 EMQX 订阅未端到端联调；gateway CoAP 未实现 |
| **成员3**<br>M2 物业管理 | workorder / visitor / parking / notice | **75%** | **workorder**：新增附件上传（MinIO）+ 附件列表接口（2 个新 logic + handler）；派单/状态流转 logic 增强。**parking**：新增 Kafka consumer.go 184 行（消费设备遥测适配）。**visitor**：checkin/checkout logic 增强。notice 2 接口+consumer 不变。 | **workorder 编译失败**：`errorx.ErrWorkOrderAssignConflict` 和 `ErrWorkOrderStatusConflict` 两个错误码常量未在 common/errorx/codes.go 定义。**parking 编译失败**：consumer.go 里 `deviceTelemetry` 结构体没有 `DeviceID` 字段（字段名不匹配） |
| **成员4**<br>M3 园区安防 | alarm / access-control / video | **85%** | **alarm**：规则引擎完整（rule/engine.go 211 行 + rule.go 360 行 + window.go）；ES 搜索（search/esclient.go 342 行）；WebSocket hub（ws/hub.go 201 行 + redisrelay）；去重冷却（dedup/cooldown.go）；通知（notify/notify.go 137 行）；DLQ 处理；大量测试（query 482 行 + window 246 行）。**access-control**：grant/revoke/list/remoteopen 四接口完整（remoteopenlogic 292 行），accesspermission_impl 188 行。**video**：摄像头管理 CRUD + HMAC-SHA256 流地址签名（streamsign.go）+ Redis 心跳状态缓存（statuscache.go + heartbeat.go 177 行）+ video_dlq 表。三服务 build 全部通过。 | alarm ES 客户端未实测连通性；WS hub 多节点广播靠 Redis relay，未压测；access-control remoteopen 与门禁硬件联动未联调；video 心跳 DLQ 消费未实现 |
| **成员5**<br>M4 能源管控 | energy-data / energy-analysis / billing | **65%** | billing cron 出账逻辑不变；energy gRPC Ping/GetDailyReport 不变。今日无新提交。 | 能源采集管道未联调 TDengine；energy-analysis 报表 logic 仍偏薄；energy gRPC 仅 Ping+1 业务方法 |
| **成员6**<br>M5 运营招商+指挥调度 | leasing / dashboard / dispatch | **70%** | **leasing**：gRPC server 完整（rpcserver/server.go 95 行 + 测试 352 行）；月度出账 cron（bill.go + 测试 119 行）。**dashboard**：WebSocket 事件增量推送（wsserver/event.go 177 行 + 测试 262 行）；缓存层（cache.go）；provider/device.go 接入。**dispatch**：派单池（assign/pool.go + 测试 54 行）；超时回收 cron（reassign.go 241 行 + 测试 250 行）；FSM 更新。 | **dispatch 编译失败**：cron/expire.go 引用 `state.ActionExpire` 但 FSM 里未定义此动作；leasing gRPC server 未被 dashboard 实际调用验证；dashboard WS 推送与 alarm WS hub 是两套实现，未统一 |
| **成员2（徐家祥）**<br>M6 公共基础+DevOps | auth / user-manage / gateway / CI | **80%** | **auth**：重构（auth.go 改 58 行 + server/authserver.go 72 行）；login/refresh/verify/validate 四接口完整。**user-manage**：RBAC CRUD 测试 181 行；rolehandler 增强；usermanageserver.go 88 行；JWT 中间件收口到 gateway。**common/errorx**：codes.go 有改动（未提交）。gateway Nacos 动态路由 + JWT 鉴权完整。 | common/errorx/codes.go 本地未提交改动——正是 workorder 报的两个错误码缺失；CI 仍无密钥扫描；gateway 与下游 IdentityFromHeader 未端到端联调记录；Dockerfile 已修 ARG SVC_DIR |

**GORM 表共 31 张**（今日新增 `video_dlq`，其余 30 张同 9-17）。

---

## 第二部分：各模块可用性 & 跨服务支撑评估

### M1 物联接入（陈建羽）
- **可启动性**：✅ build 通过
- **CRUD 可用性**：device 10 HTTP + gRPC 4 方法可用；shadow 三方法带乐观锁
- **支撑能力**：event-dispatcher → alarm/workorder/dispatch 的 Kafka 上游；shadow gRPC → dashboard/其他服务读影子
- **卡点**：EMQX 订阅未联调，主链路"设备→Kafka"这一段没实测

### M2 物业管理（成员3）
- **可启动性**：❌ workorder 编译失败（2 个错误码未定义）、parking 编译失败（结构体字段名不匹配）
- **CRUD 可用性**：visitor 4 接口可用；notice 2 接口可用；workorder/parking 修完编译错误后可用
- **支撑能力**：workorder 消费 alarm-event 自动建单；parking 消费设备遥测
- **卡点**：**workorder 编译错误是跨模块问题**——`ErrWorkOrderAssignConflict` 和 `ErrWorkOrderStatusConflict` 应该加在 common/errorx/codes.go，这是 M6 成员2 的文件，需要徐家祥补

### M3 园区安防（成员4）
- **可启动性**：✅ build 通过（alarm/access-control/video 全绿）
- **CRUD 可用性**：alarm 完整（规则引擎+ES+WS+通知+DLQ）；access-control 四接口完整；video 摄像头 CRUD+HMAC+心跳缓存
- **支撑能力**：alarm WS hub → dashboard 大屏推送；alarm gRPC GetActiveAlarms → dashboard 聚合；video HMAC 流地址 → 前端播放
- **卡点**：ES 客户端未连通；WS hub 与 dashboard wsserver 两套推送实现未统一；video 心跳 DLQ 只写了模型没写消费逻辑

### M4 能源管控（成员5）
- **可启动性**：✅ build 通过
- **CRUD 可用性**：billing 出账 cron 可用；energy gRPC 2 方法
- **支撑能力**：energy gRPC GetDailyReport → dashboard 大屏
- **卡点**：能源数据采集未联调 TDengine；energy-analysis 报表逻辑薄

### M5 运营招商+指挥调度（成员6）
- **可启动性**：❌ dispatch 编译失败（`state.ActionExpire` 未定义）
- **CRUD 可用性**：leasing gRPC server 完整（带测试）；dashboard WS 推送完整（带测试）；workorder 派单池+超时回收逻辑完整但编译不过
- **支撑能力**：leasing gRPC → dashboard 聚合；dispatch 消费 alarm-event 建单；dashboard WS → 前端大屏
- **卡点**：dispatch FSM 缺 ActionExpire 动作定义；dashboard WS 与 alarm WS 未统一为一套

### M6 公共基础+DevOps（成员2/徐家祥）
- **可启动性**：✅ auth/user-manage/gateway build 全部通过
- **CRUD 可用性**：auth 四接口完整；user-manage RBAC CRUD + 测试完整；gateway JWT 鉴权 + Nacos 动态路由完整
- **支撑能力**：gateway JWT → 所有下游服务统一鉴权；auth login → 全员入口；user-manage → 用户/角色管理
- **卡点**：**common/errorx/codes.go 缺两个错误码**（ErrWorkOrderAssignConflict / ErrWorkOrderStatusConflict），这是 workorder 编译失败的直接原因；CI 无密钥扫描；网关与下游 IdentityFromHeader 未联调

---

## 第三部分：当日整体核心目标

**一句话目标**：修掉 3 个编译错误（dispatch FSM 动作 / parking 结构体字段 / workorder 错误码），打通「设备上报 → event-dispatcher → alarm 规则引擎 → WS 推送 → dashboard 大屏」主链路。

**主链路路径**：
```
设备 MQTT → event-dispatcher → Kafka alarm-event
  → alarm-service（规则引擎匹配）→ MySQL 落库 + ES 索引
  → alarm WS hub / Redis relay → dashboard wsserver
  → 浏览器 WebSocket 实时推送
```

---

## 第四部分：分成员细分任务

### 陈建羽（M1 物联接入）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | event-dispatcher 与 EMQX 端到端联调：模拟 MQTT 上报，确认 Kafka alarm-event 收到 | 联调记录 + Kafka 消息截图 | 2h |
| **P0** | device register 联调：注册一台摄像头，确认 device 表落库 + shadow reported 初始化 | 联调记录 + 表数据截图 | 1.5h |
| P1 | device 表补 type/lat/lng 列，修 gRPC GetDevice 零值 TODO | DDL + gRPC 返回真实值 | 1.5h |
| P2 | gateway CoAP 协议解析 | 协议文档 | 排期 |

### 成员3（M2 物业管理）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | 修 parking consumer.go：`deviceTelemetry` 结构体字段名对齐（查 device-service 实际发的 Kafka message schema，改成对应字段） | parking-service build 通过 | 1h |
| **P0** | workorder 附件上传端到端验证：上传一个附件，确认 MinIO 落库 + work_order_attachment 表有记录 | 联调记录 + 截图 | 1.5h |
| P1 | visitor checkin/checkout 与 access-control remoteopen 联调：访客登记 → 开门授权链路 | 联调记录 | 2h |
| P2 | parking 计费与 notice 联动 | 设计文档 | 排期 |

### 成员4（M3 园区安防）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | alarm-service 规则引擎端到端验证：造一条设备遥测，确认规则匹配 → alarm 落库 → WS 推送 | 联调记录 + WS 消息截图 | 2h |
| **P0** | alarm ES 客户端连通性验证：确认 ES 在跑、索引建好、告警能写入 | 验证记录 + curl 截图 | 1h |
| P1 | video 心跳 DLQ 消费逻辑补全（当前只有模型没 consumer） | consumer 代码 | 2h |
| P1 | access-control remoteopen 与 M1 网关下发指令联调 | 联调记录 | 1.5h |
| P2 | alarm WS hub 与 dashboard wsserver 统一为一套推送 | 设计方案 | 排期 |

### 成员5（M4 能源管控）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | billing 月度出账 cron 跑一次：确认能读 energy_reading → 生成 bill 记录 | 测试记录 + bill 表数据 | 2h |
| P1 | energy gRPC GetDailyReport 与 dashboard provider 对接验证 | 联调记录 | 1.5h |
| P2 | energy-analysis 报表 logic 补全 | logic 代码 | 排期 |

### 成员6（M5 运营招商+指挥调度）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | 修 dispatch FSM：在 state/fsm.go 里补 `ActionExpire` 动作定义（超时回收对应的状态迁移） | dispatch-service build 通过 | 1h |
| **P0** | dispatch 超时回收 cron 端到端验证：建一个 dispatch_task，等 30s 确认自动回收 | 联调记录 + task_log 截图 | 1.5h |
| P1 | leasing gRPC server 被 dashboard 实际调用验证 | 联调记录 | 1.5h |
| P2 | dashboard WS 推送与 alarm WS hub 统一 | 设计方案 | 排期 |

### 成员2（徐家祥，M6 公共基础+DevOps）

| 优先级 | 工作内容 | 交付产出 | 预计工时 |
|---|---|---|---|
| **P0** | **common/errorx/codes.go 补两个错误码**：`ErrWorkOrderAssignConflict` 和 `ErrWorkOrderStatusConflict`（workorder 编译失败的直接原因），提交 | codes.go 更新 + workorder build 通过 | 0.5h |
| **P0** | gateway JWT 与下游 IdentityFromHeader 端到端联调：带 token 调 /api/users，确认下游 ctxdata 能取到 user_id | 联调记录 + curl + 下游日志 | 2h |
| **P0** | user-manage RBAC 接口经网关调通：建用户 → 分配角色 → 带 token 调业务接口验证权限 | 联调记录 | 2h |
| P1 | 提交 common/errorx/codes.go 当前未提交的本地改动 | git commit | 0.5h |
| P1 | CI 加密钥扫描步骤（gitleaks 或 grep 硬编码密码） | CI yml 更新 | 1.5h |
| P2 | 种子管理员 SQL（deploy/sql/seed_admin.sql） | SQL 文件 | 1h |

---

## 第五部分：全员统一时间安排

| 时段 | 时间 | 内容 |
|---|---|---|
| **上午（开发）** | 09:15–12:00 | 各自 P0 修复与开发；徐家祥 09:30 前补完错误码通知成员3 重跑 workorder build；成员6 10:00 前补完 ActionExpire 通知重跑 dispatch build |
| **下午（联调）** | 14:00–17:30 | 跨模块联调：陈建羽+成员4 联调 MQTT→Kafka→alarm→WS；成员2+成员3 联调网关→auth→user-manage；成员6 联调 leasing gRPC→dashboard |
| **晚间（复盘）** | 17:30–18:00 | 每人报：今日完成 P0 项 / 未完成原因 / 明日计划；遗留问题更新台账 |

---

## 第六部分：当日统一验收标准

### 单模块验收
- go build ./... 通过（当前 14/17，今日目标 17/17）
- 至少 1 个核心接口 curl 可调通（返回 200 + 数据落库）
- 新增代码有对应单测（alarm/dispatch/leasing/dashboard 今日新增代码均已有测试文件）

### 全链路验收
```
设备 MQTT 上报 → event-dispatcher → Kafka alarm-event
→ alarm 规则引擎匹配 → MySQL + ES 落库
→ alarm WS hub → dashboard wsserver → 浏览器收到推送
```
端到端跑通 = 从发一条 MQTT 消息到浏览器大屏实时看到告警卡片。

### 遗留问题台账

| # | 问题 | 负责人 | 预计解决 |
|---|---|---|---|
| 1 | dispatch cron/expire.go 引用未定义的 state.ActionExpire | 成员6 | 今日上午 |
| 2 | parking consumer.go deviceTelemetry 结构体无 DeviceID 字段 | 成员3 | 今日上午 |
| 3 | workorder 缺 ErrWorkOrderAssignConflict / ErrWorkOrderStatusConflict 错误码 | 成员2 | 今日上午 |
| 4 | alarm ES 客户端未连通性验证 | 成员4 | 今日下午 |
| 5 | alarm WS hub 与 dashboard wsserver 两套推送未统一 | 成员4/成员6 | 下周 |
| 6 | event-dispatcher 与 EMQX 未端到端联调 | 陈建羽 | 今日下午 |
| 7 | gateway JWT 与下游 IdentityFromHeader 未联调 | 成员2 | 今日下午 |
| 8 | CI 无明文密钥扫描 | 成员2 | 今日 P1 |
| 9 | video 心跳 DLQ 只写模型未写 consumer | 成员4 | 今日 P1 |
| 10 | device 表缺 type/lat/lng 列 TODO | 陈建羽 | 今日 P1 |
| 11 | access-control remoteopen 与门禁硬件未联调 | 成员4 | 下周 |
| 12 | energy TDengine 采集管道未联调 | 成员5 | 下周 |
